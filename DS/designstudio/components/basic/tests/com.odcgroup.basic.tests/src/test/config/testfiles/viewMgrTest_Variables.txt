*** Please do not edit this file ***    
    SUBROUTINE VariableListView
*Subroutine used in test
$INSERT I_COMMON
$INSERT I_EQUATE

    GOSUB init;* Init
    GOSUB process ;*:prcess

    RETURN

init:
    record = '1':VM:'2':VM:'3':VM:'4':VM:'5':VM:'6':VM:'7'
    key = "Variable"
    count = 100
    average = 34.10000
    tempKey = key
    temperature = -40.4
    tempRecord = record<1>

    RETURN
*-------------------------------------
process: ;* Process
    time = TIMESTAMP()
    LOOP
    UNTIL count LE 0 DO
        tempKey = key + "100" + temperature
        record<1,8> = "'EI'":"'GHT'" ;* Test
        IF ((count - average)+time/3.12) NE (average - temperature -1) THEN
            CONTINUE
        END ELSE
            CRT "Test finished!"
        END
    REPEAT

    RETURN
    END
