 Logger=debug(sContext, msg):error(sContext, msg):fatal(sContext, msg):info(sContext, sMessage):isDebugEnabled(sContext):log(sContext, msg, category):trace(sContext, msg):warn(sContext, msg):
 System=fetchFromCache(table, key):getVariable(variableName):getVariableForSelection(variableName):
 Util=CacheRead(fnFileName,sContext):CreateUniqueId:logwrite(fnFileName, recId, rRecord):write(fnFileName, recId, rRecord):
