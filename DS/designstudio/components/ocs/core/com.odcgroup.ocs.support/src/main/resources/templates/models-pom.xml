<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<!-- This is needed so that the 'ds-models' packaging is understood -->
  	<parent>
    	<groupId>com.odcgroup.odp</groupId>
    	<artifactId>ocs-core</artifactId>
    	<version>${ds.target.platform.version}</version>
	</parent>
    <groupId>${artifactId}</groupId>
	<artifactId>${artifactId}</artifactId>

  	<packaging>ds-models</packaging>

    <dependencies>
		<!-- example how to define a dependency on another model project
		<dependency>
			<groupId>com.odcgroup.ocs.cbi</groupId>
			<artifactId>cbi-models</artifactId>
			<version>${project.version}</version>
		</dependency>
		-->
     
		<dependency>
		  <groupId>com.odcgroup.ds.el</groupId>
		  <artifactId>el-runtime</artifactId>
		  <version>${project.version}</version>
		</dependency>
		<dependency>
		  <groupId>org.eclipse.xtext</groupId>
		  <artifactId>org.eclipse.xtext.xbase.lib</artifactId>
		  <version>2.5.3</version>
		</dependency>
    </dependencies>

</project>
