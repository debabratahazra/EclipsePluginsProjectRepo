# General
name=case4
description=Case 4 template, with ordered parameters
autowireModulePom=custo/pom.xml
parametersOrder=first,second,third

# Parameters
# Syntax:
# someParam.defaultValue=abc
# someParam.mandatory=true
# someParam.description=Description of the parameter

# first parameter
first.defaultValue=First default value
first.mandatory=true
first.description=First description

# second parameter
second.description=Second description (no default value, no mandatory parameter)

# third parameter
third.mandatory=false
third.description=Third description (no default value, not mandatory)
