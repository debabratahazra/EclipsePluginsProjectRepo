// This is not a real Xtext grammar;
// it's just so that ExampleGeneratorTest can load SOME Xtext resource
// (and a *.xtext is itself based on Xtext..) - without workbench
// having to depend on real domain etc. features (layering).
// @author Michael Vorburger

grammar com.odcgroup.ds.Test

generate enquiry "http://www.odcgroup.com/ds/Test"

Test: 'hello, world' ;
