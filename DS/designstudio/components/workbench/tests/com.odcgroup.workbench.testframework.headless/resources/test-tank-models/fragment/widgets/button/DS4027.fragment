# UTF-8
metamodelVersion = 8.0.0.20130123

--- Fragment ---
	cardinality = one
{
	--- Box ---
		boxType = vertical
	{
		--- Button ---
			id = btn_f8_8_
		{
			Event OnBlur (pullModule)
			{
				param = param-value
				ref-id = ref-id-value
				scope-id = scope-id-value
				uri = uri-value
				variant = refresh
				widget-group-ref = widget-group-ref-value
				param-name-1 = param-value-1 ud
				param-name-2 = param-value-2 ud
			}
			Event OnChange (pullModule)
			{
				param = param-value
				ref-id = ref-id-value
				scope-id = scope-id-value
				uri = uri-value
				variant = refresh
				widget-group-ref = widget-group-ref-value
				param-name-1 = param-value-1 ud
				param-name-2 = param-value-2 ud
			}
			Event OnClick (pullModule)
			{
				param = param-value
				ref-id = ref-id-value
				scope-id = scope-id-value
				uri = uri-value
				variant = refresh
				widget-group-ref = widget-group-ref-value
				param-name-1 = param-value-1 ud
				param-name-2 = param-value-2 ud
			}
			Event OnEnter (pullModule)
			{
				param = param-value
				ref-id = ref-id-value
				scope-id = scope-id-value
				uri = uri-value
				variant = refresh
				widget-group-ref = widget-group-ref-value
				param-name-1 = param-value-1 ud
				param-name-2 = param-value-2 ud
			}
			Event OnSelect (pullModule)
			{
				param = param-value
				ref-id = ref-id-value
				scope-id = scope-id-value
				uri = uri-value
				variant = refresh
				widget-group-ref = widget-group-ref-value
				param-name-1 = param-value-1 ud
				param-name-2 = param-value-2 ud
			}
		}
	}
}