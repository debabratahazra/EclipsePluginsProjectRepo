After creating IRIS Embedded Server for TAFJ you need to follow below steps

1. Set TAFJ_HOME inside Design Studio
2. Set JBOSS_HOME inside Design Studio
3. SET override.tafj.home in web-server.properties(src/main/resources)
