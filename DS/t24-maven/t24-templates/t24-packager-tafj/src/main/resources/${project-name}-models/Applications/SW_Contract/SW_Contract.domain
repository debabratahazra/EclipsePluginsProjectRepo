# UTF-8
# UTF-8
/* Termplate Servicing of Swaps */
Domain SW_Contract
	@t24:i (module=SW)
	@java:Package (value=com.temenos.t24.datamodel.sw.sw_contract)
namespace "http://www.temenos.com/t24/SW_Contract"
metamodelVersion 1.30.6
Classes {
	/* This is the main Swap contract file.
The details supporting the swap transaction will be held on a single contract. A Swap contract consists of two legs, Asset and Liability, both of which are maintained on the contract. It stores details of the counterparty of the swap, the notional principal(s), rates, schedules for both swap legs, payment instructions etc. A swap contract is an agreement to exchange interest payments in a fixed obligation for interest payments in a floating rate obligation (Interest Rate Swap), or one currency for another (Currency Swap), and reverse the exchange at a later date.
An Interest Rate Swap (IRS) contract is a contract in which two counterparties agree to exchange interest payments of differing character based on an underlying notional principal amount that is never exchanged.
In a Currency Interest Rate Swap (CIRS) contract the underlying principal amount is usually real (not notional) and is usually exchanged (Principal Exchange PX and Principal Re-exchange RX schedules).
Typically, a swap contract exchanges fixed rate obligations for a floating rate instrument in the same currency for an IRS contract and different currencies for a CIRS contract.
In its simplest form, the two parties to an Interest Rate Swap exchange the interest payment obligations (IRS - no principal changes hands, CIRS - principal changes hands) on two different kinds of debt instruments, one being a fixed interest rate, the other being a floating rate.
The following are examples of Interest Swaps :

Coupon swap or exchange of fixed rate for floating rate instruments in the same currency
Basis swaps or the exchange of floating rate to floating rate instruments in the same     currency.
Cross currency interest rate swaps involving the exchange of fixed rate instruments in     one currency for floating rate in another.
Exchange of floating rate to floating rate instruments in different currencies.
For example : (See Figure Below)

Notional Amount : $10 Million
Maturity: 5 Years
Payment Freq. : Both fixed and floating rate payments made semi-annually.
A Foreign Exchange (FOREX) swap differs from an Interest Rate Swap (See Foreign Exchange module for more information on FOREX swaps). A Foreign Exchange swap contract within the FOREX application is the simultaneous buying and selling of a currency in approximately equal amounts for different maturity dates. The swap price is the difference in price between the two maturity dates of the swap. */
	SWAP
		@t24:i (t24Name=SWAP, level=F, type=H, additionalInfo="NOH OEU UNP OEU", allowedFunctions="A C D E H I L P Q R S V", localRefAllowed=true)
	{
		/* A unique reference number which identifies a particular Swap contract.
Contract numbers can be generated either manually or automatically. Where automatic generation is supported depression of the F3 key at the "Awaiting ID" prompt supplies the next available contract number.
To avoid the necessity to manually input the complete reference number (contract number), the User has the facility to input only the sequence number of the transaction which has been input that day. The Application will automatically generate the seven first digits and append them to the front of the sequence number in order to retrieve the requested transaction. Access to any Swap transaction from a previous day requires the input of the corresponding julian day and year if the contract has been done in a previous year.
The reference has the following format SWyydddnnnnn, where :
o SW = Application indicator for the Swaps application. o yy = The last two digits of the year when the transaction is processed. o ddd = Julian day number within the year when the Swap transaction is processed. o nnnnn = Five digit sequential number.
The above numbering convention enables 99,999 deals to be struck on any one day.
When a deal is to be confirmed (using the Verify function) the type of confirmation (either customer or broker) may be defined by prefixing either CU (for customer confirmations) or BR (for broker confirmations) to the contract number of the deal to be confirmed. If this prefix is missing and the deal is to be verified the user will be requested to specify which type of confirmation is required. Once the prefix is chosen it remains the same until changed (i.e. the prefix can be specified once and then omitted for subsequent contracts).
Certain versions will automatically append the BR or CU prefix to the contract number. If the prefix BR or CU is present during any other function it will be removed from the contract number and will have no effect on the processing of the contract.
Validation Rules

10 numeric characters, prefixed by "SW" :         SWyydddnnnnn - SW followed by 10N (See details).         (Mandatory Input).
If the reference number is entered, during initial input,         it will be validated for uniqueness.
When input, this field will only be allowed in the format         above.
This transaction number can never be changed. */
		CONTRACT_NUMBER -> SW_Contract:SWAP PK required
			@i18n:Label (en="Contract Number")
			@t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=0.0, core=true, businessType=A, t24Name=CONTRACT.NUMBER)
		/* Defines the type of Swap contract.
All Swap types allowed on the system are defined in the SWAP.TYPE file.  It holds the description of the Swap type, the forward rate methods, etc.
Validation Rules
Any valid Swap type defined on the SWAP.TYPE file.
1-6 alpha-numeric characters. (Mandatory input). */
		SWAP_TYPE -> SW_Config:SWAP_TYPE required
			@i18n:Label (en="Swap Type")
			@t24:i (align=L, inputBehaviour=F, maxLength=7, sysNumber=1.0, core=true, businessType=A, t24Name=SWAP.TYPE)
		/* Identifies the counterparty with whom the Bank makes the contract.
This is the identification assigned to the Customer on first contact with the bank.  The customer will be referred to by this identification in all subsequent operations with the Bank.  The system will make use of this number to:
a)         Identify the Counterparty of the transaction engaged in            by the Bank,
b)         Print the Customer's name and address on all contracts            and/or advices produced by this module,
c)         Locate the liability number for limit checking on            Placement contracts.
Validation Rules
3-10 alphanumeric customer mnemonic or, 1-10 numeric character customer number.  (Mandatory input).
Must be a valid Customer number or mnemonic.
Once the contract has been authorised, this field cannot be changed. */
		CUSTOMER -> ST_Customer:CUSTOMER required
			@i18n:Label (en=Counterparty)
			@i18n:Tooltip (en=Counterparty)
			@t24:i (align=R, inputBehaviour=F, maxLength=10, sysNumber=2.0, core=true, businessType=CUS, t24Name=CUSTOMER)
		/* Identifies the dealer desk relating to the transaction.
Each deal on the system will be allocated a two-digit dealer desk code.  This code will be used to maintain position at the dealer desk level.
Validation Rules
2 numeric characters. Mandatdory field. Default taken from user profile (Field 41), Input Optional.
Must be an existing code on the DEALER.DESK table. */
		DEALER_DESK -> ST_Config:DEALER_DESK
			@i18n:Label (en="Dealer Desk")
			@i18n:Tooltip (en="Specifies the Dealer Desk handling the transaction")
			@t24:i (align=L, inputBehaviour=F, maxLength=2, sysNumber=3.0, core=true, businessType=A, t24Name=DEALER.DESK)
		/* Identifies which Currency Market is accessed for this transaction.
The use of this field will only be applicable for those countries where the exchange authorities have defined different markets for foreign currency dealing.
A typical example would be Belgium where foreign currencies are dealt with on the Regular Market and also on the Free Market.
The default value of '1' means that, for the majority of countries, where only one exchange rate market exists, input in this field will not be necessary.

Note:      In the present release, only one market can be defined on            the CURRENCY.MARKET table.  Consequently, for the            time being, only the value of '1' will be accepted in this            field.
Validation Rules
One Numeric digit Currency Market code. (No input.  System value is 1.)
Any input in this field must be a valid code on the CURRENCY.MARKET table. */
		CURRENCY_MARKET -> FIN_CurrencyConfig:CURRENCY_MARKET
			@i18n:Label (en="Currency Market")
			@i18n:Tooltip (en="Identifies Currency Market for the transaction")
			@t24:i (align=R, inputBehaviour=F, maxLength=1, sysNumber=4.0, core=true, t24Name=CURRENCY.MARKET)
		/* Identifies the position type applicable to the swap transaction.
This field is not used at present and is reserved for future use.
Validation Rules
2 alpha-numeric characters.
No-input field.  System default "TR" (Trading). */
		POSITION_TYPE : T24BusinessTypes:A
			@i18n:Label (en="Position Type")
			@i18n:Tooltip (en="Position Type")
			@t24:i (align=L, inputBehaviour=N, maxLength=2, sysNumber=5.0, core=true, t24Name=POSITION.TYPE)
		/* Represents the actual day that the swap transaction was agreed between all parties to the contract.
Contains the actual date that the deal was struck.
This date should not be confused with the value date of the contract.
Validation Rules
1-9 type A (alphanumeric) characters. (Optional Input).
An override is required should the date not be defined or is defined as a non-working day on the Holiday table.
This date must be equal to or prior to the value date of the contract.
Default today. */
		TRADE_DATE : T24BusinessTypes:D
			@i18n:Label (en="Trade Date")
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=6.0, core=true, t24Name=TRADE.DATE)
		/* Contains the value date of the contract and is therefore the date from which the system will start to accrue interest.  This date must be prior to the Maturity Date of the Contract such that the system may calculate interest.

Validation Rules
Type DD (standard date format in range 1000 - 2049). (Optional input; Default value = Run date).
The system will automatically access the HOLIDAY table to ensure that the value date is a working day and not a holiday in the country associated to the currency of the contract.  Any Holiday will require an override. */
		VALUE_DATE : T24BusinessTypes:D
			@i18n:Label (en="Value Date")
			@i18n:Tooltip (en="Indicates the Value date of the Contract")
			@t24:i (align=R, inputBehaviour=F, maxLength=11, sysNumber=7.0, core=true, t24Name=VALUE.DATE)
		/* Identifies when the swap contract is due to terminate.
This field represents the latter date of when the principals are re-exchanged as scheduled, (AS/LB.TYPE = RX,CM) or when the final interest payment is made.
The asset and liability dates for the re-exchange of principals or interest payments  may not always be the same.
The Maturity Date is the date on which the deal will be liquidated.  At liquidation, all the corresponding amounts of principal (if appropriate) and interest will be passed to the appropriate accounts as designated within the transaction.
Validation Rules
Type DD (standard date format in range 1000 - 2049) or, 3 numeric characters representing a number of days: (Mandatory input)
The date will be checked against the HOLIDAY table. Any non-working day will require an override prior to acceptance.
The maturity date must be greater than the value date.
This date cannot be changed after authorisation when INTEREST BASIS = 'S' (Special).
For Asset type contract, an override will be required when this date is smaller than the EXPIRY DATE of the corresponding Credit line. */
		MATURITY_DATE : T24BusinessTypes:MATLMM required
			@i18n:Label (en="Maturity Date")
			@i18n:Tooltip (en="Indicates the Maturity Date of the Transaction")
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=8.0, core=true, t24Name=MATURITY.DATE, typeModifiers="74070")
		/* This field specifies the revaluation type for the Swap NPV Revaluation.
Validation Rules
1-13 Alpha-numeric characters. (Default from SWAP.TYPE>REVAL.TYPE) No-input field.
Must be null if SWAP.TYPE>INT.CAT.REVAL.LOSS is null. */
		REVALUATION_TYPE : SW_Contract:SWAP__REVALUATION_TYPE
			@i18n:Label (en="Revaluation Type")
			@i18n:Tooltip (en="Revaluation method to be used on forward contracts")
			@t24:i (align=L, inputBehaviour=I, maxLength=13, sysNumber=9.0, core=true, t24Name=REVALUATION.TYPE)
		/* Swap Trade type.
Validation Rules
1-5 Alpha-numeric characters.
Valid types are "TRADE" and "HEDGE".  (Optional input.  Default = "TRADE"). */
		TRADE_TYPE : SW_Contract:SWAP__TRADE_TYPE
			@i18n:Label (en="Trade Type")
			@i18n:Tooltip (en="If the deal is a Trade deal or Hedge deal")
			@t24:i (align=L, inputBehaviour=I, maxLength=5, sysNumber=10.0, core=true, t24Name=TRADE.TYPE)
		/* Defines the customer's own reference number.
The reference number input in this field will be printed on all advices and confirmations etc. with the bank's contract number.
This field may be changed at any time in the life of the contract.
Validation Rules
1-16 alphanumeric characters.
Must be valid Swift characters.
Free format client reference. */
		CUSTOMER_REF : T24BusinessTypes:S
			@i18n:Label (en="Customer Ref")
			@t24:i (align=L, inputBehaviour=I, maxLength=16, sysNumber=11.0, core=true, t24Name=CUSTOMER.REF)
		/* Identifies the LIMIT REFERENCE number to be checked for Limit control on transactions.
Specifies the Credit Line number under which the transaction was granted.
If no input is made in this field, then the Limits system will provide a default value according to the Limit Reference record which  has its field application identifier set tyo "SW".  If no match is found in selective Reference, the default value will be the Reference with its field reference blank.
The credit line number generated will be NNNN-01 where NNNN is the Id of the Limit Reference record described above.
Validation Rules
3 - 10 numeric character LIMIT REFERENCE code. (Optional Input.  Default value).
Input in this field must be a valid LIMIT REFERENCE number.
If no Limit exists for the counterparty a corresponding error message will be displayed by the system with an override facility.
If the transaction amount exceeds the availability amount of the Limit, a corresponding error message will be displayed with an override facility. */
		LIMIT_REFERENCE : mml:Integer
			@i18n:Label (en="Limit Reference")
			@i18n:Tooltip (en="Indicates the limit that will be updated")
			@t24:i (align=R, inputBehaviour=I, maxLength=10, sysNumber=12.0, core=true, t24Name=LIMIT.REFERENCE)
		/* Identifies whether the Asset and Liability payments and receipts are to be netted.
Depending on the resulting movement, ie pay or receive, an entry is created based on the contract settlement instructions.
Note that CRF entries cannot be netted.
Validation Rules
1-2 Alpha characters. Valid values are "Y" and "NO". (Default = "NO"). */
		NET_PAYMENTS : SW_Contract:SWAP__NET_PAYMENTS
			@i18n:Label (en=Netting)
			@i18n:Tooltip (en="Whether to Net Asset and Liability payments")
			@t24:i (align=L, inputBehaviour=I, maxLength=2, sysNumber=13.0, core=true, t24Name=NET.PAYMENTS)
		/* Defines the Transaction link reference of the Swap contract.
The TRANS.LINK.REFERENCE file enables transactions to be linked together for reporting purposes.
Eg. A Swap contract to a backing Loans and Deposits contract.

The updating of the file is fully automatic :
o  If the user enters a reference id that does not exist on the    reference file, a new record is created, and the contract Id    concerned is held.
o  If the user changes the reference id, the contract Id concerned    is moved to the new reference record.  Should no other contracts    exist on the reference, then that reference is removed.
Validation Rules
1-12 Alpha-numeric characters. (Optional input). */
		LINK_REFERENCE : T24BusinessTypes:A
			@i18n:Label (en="Link Reference")
			@i18n:Tooltip (en="Transaction link reference of the contract")
			@t24:i (align=L, inputBehaviour=I, maxLength=12, sysNumber=14.0, core=true, t24Name=LINK.REFERENCE)
		/* Identifies the account officer respnsible for the relationship with the counterparty. This information will be used by the Management Information System (MIS) for the determination of the Customer Profitability Analysis.
This field has been provided to enable the user to override at transaction level the counterparty default account officer defined on the Customer file.
Validation Rules
1-3 Numeric characters. Optional input. (Default value = Account Officer specified on the counterparty's Customer file).
Must be defined on the DEPT.ACCT.OFFICER table. */
		ACCOUNT_OFFICER -> ST_Config:DEPT_ACCT_OFFICER
			@i18n:Label (en="Account Officer")
			@i18n:Tooltip (en="Indicates Account Officer responsible")
			@t24:i (align=R, inputBehaviour=I, maxLength=4, sysNumber=15.0, core=true, businessType=DAO, t24Name=ACCOUNT.OFFICER)
		/* Identifies the customer portfolio number to which the Swap contract will be associated with. This will be used for the portfolio valuation for the customer.  No input will result in the deal to be excluded from the portfolio valuation.
Validation Rules
Upto 3 Numeric characters. (Optional input). */
		PORTFOLIO_NUMBER : mml:Integer
			@i18n:Label (en="Portfolio Number")
			@i18n:Tooltip (en="Portfolio Number")
			@t24:i (align=R, inputBehaviour=I, maxLength=3, sysNumber=16.0, core=true, t24Name=PORTFOLIO.NUMBER)
		/* Defines the swap contract Agreement type. If a matster agreement type is entered then the default full agreement type (eg TYPE / DATE // VERSION) will be used on the SWAP contract if such an agreement type has been entered on the master SWAP.AGREEMENT.TYPE record.
Eg. ISDA  (International Swap Dealers Association),     BBAIRS, MASTER, etc.

This is also used to define Swift Message conditions. Corresponds to field 77 on Swift messages.
Validation Rules
1-12 Alphanumeric characters. (Mandatory input).
Must be a valid definition on the SWAP.AGREEMENT.TYPE file.
Each contract must be linked to an agreement type. */
		AGREEMENT_TYPE -> SW_Config:SWAP_AGREEMENT_TYPE required
			@i18n:Label (en="Agreement Type")
			@i18n:Tooltip (en="Agreement Type Eg. ISDA, FRABBA, etc.")
			@t24:i (align=L, inputBehaviour=I, maxLength=22, sysNumber=17.0, core=true, businessType=SWAG, t24Name=AGREEMENT.TYPE)
		CONDITIONS -> byValue SW_Contract:SWAP__CONDITIONS *
		/* Contains the Product Category Code to which the transaction will be assigned.
The Category code must be within the range :
o  20000 - 49999
Validation Rules
5 Numeric character Category Code. (Mandatory input.  No Default value).
Must be defined on the CATEGORY table.
Once the contract has been authorised, the contract Category code can only be changed to another number within the original Category range. */
		PRODUCT_CATEGORY -> ST_Config:CATEGORY
			@i18n:Label (en="Product Category")
			@t24:i (align=R, inputBehaviour=I, maxLength=5, sysNumber=19.0, core=true, t24Name=PRODUCT.CATEGORY)
		BANK_INFO -> byValue SW_Contract:SWAP__BANK_INFO *
		/* The field will either hold values NULL or NO. By default it will hold NULL value, meaning that the customer in the deal is a treasury customer.  It can be set to NO at the input level, wherein the interest rate charged to the customer is different from the treasury interest rate. Once this field is set to NO, then it becomes mandatory to input the treasury interest rate in the asset or liability side or both. When both the asset and liability side interest rate is set to floating, then TREASURY.CUSTOMER cannot be set to NO.The value in the field cannot be changed from NULL to NO or vice versa after authorization of the SWAP contract.
The following input will be allowed in this field:

'NULL' = which means that the CPARTYof the deal is a Treasury
Customer.
'NO'   = which means that the CPARTYof the deal is not a
Treasury Customer.

(Optional input; default value = Null, i.e. the CPARTY is a
Treasury Customer.) */
		TREASURY_CUSTOMER : SW_Contract:SWAP__TREASURY_CUSTOMER
			@i18n:Label (en="Treasury Customer")
			@i18n:Tooltip (en="Indicates whether treasury customer?")
			@t24:i (align=L, inputBehaviour=F, maxLength=2, sysNumber=21.0, core=true, t24Name=TREASURY.CUSTOMER)
		/* Indicates the currency of the asset side of the swap contract.
This field indicates the currency of the asset side of the swap contract and will be the same as the currency of the various accounts where the accounting movements in respect of the contract will be debited or credited.
Validation Rules
3 type SSS (uppercase alpha) characters. or     1-3 numeric character currency code. (Optional input; Default = local currency).
The currency code must exist on the CURRENCY table.  Any existing currency code will be accepted and the name redisplayed for checking.
Once the contract has been authorised this field cannot be changed. */
		AS_CURRENCY -> FIN_CurrencyConfig:CURRENCY
			@i18n:Label (en=Currency)
			@i18n:Tooltip (en="Currency in the asset side of the swap contract.")
			@t24:i (align=L, inputBehaviour=F, maxLength=3, sysNumber=22.0, core=true, businessType=CCY, t24Name=AS.CURRENCY)
		/* The Principal amount of the Asset side of the swap contract which will always be expressed in the currency defined in the previous field.
In order to facilitate the efficient entry of the amount, an input feature has been provided which allows for the suppression of trailing zeros.
In English Language text this is accomplished by using a 'T' to indicate the thousands and an 'M' to indicate the millions. Other languages will require the provision of translation values.
E.g.       INPUT              VALUE
1T               1,000.00              2M               2,000,000.00
Validation Rules
1-19 type AMT (standard amount format) characters plus a decimal point. (Mandatory input) The amount must be greater than Zero. */
		AS_PRINCIPAL : T24BusinessTypes:AMT
			@i18n:Label (en=Principal)
			@i18n:Tooltip (en="Principal amount in Asset side of swap contract")
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=23.0, core=true, t24Name=AS.PRINCIPAL, typeModifiers="22")
		/* Identifies the start date of the first interest period and therefore the true Value date of the Asset leg.
It is the date on which interest accruals commence on the outstanding balance of the contract.
This date can be entered as either the current date (today) or a date in the past (less than the contract value date).  It cannot be greater than the contract value date.
If forward dated, ie. both the value date and interest effective date are forward, then the contract status will be null until the effective date is reached, at which time, the principal amount will be drawn down and the contract status changed to "CUR".
An input of a null value will result in the date being defaulted to the contract value date.
If backdated or 'today' dated then the contract will be drawn down, put on-line, and the contract status set to "CUR".
If backdated into a prior month, interest will be accrued on-line up to the end of the previous month.
Input of a non working day will require an override.
Validation Rules
Standard Date format.
Defaults to the contract Value Date.
Cannot be greater than the contract Value Date.
Input is prohibited was the contract status has been set to "CUR" (Current).
An override will be required if this date is a non-working day. */
		AS_INT_EFFECTIVE : T24BusinessTypes:D
			@i18n:Label (en="Effective Date")
			@i18n:Tooltip (en="Identifies start date of the first interest period")
			@t24:i (align=R, inputBehaviour=F, maxLength=11, sysNumber=24.0, core=true, t24Name=AS.INT.EFFECTIVE)
		/* Specifies the fixed interest rate at which interest is to be accrued on the Asset leg.
Once the contract has been drawn down, ie. the contract status is "CUR", this field cannot be changed.  However, the interest rate can be changed at any time via the definition of a Rate Reset ("RR") schedule.
Validation Rules
1-11 numeric characters. (Optional Input).
Input is prohibited if data is present in the fields AS.RATE.KEY and AS.SPREAD.
A rate must be present in this field if the fields AS.RATE.KEY and AS.SPREAD are null.
Field will be no-input once the contract has reached the Interest effective date, ie. contract status = "CUR". */
		AS_FIXED_RATE : mml:Integer
			@i18n:Label (en="Fixed Rate")
			@i18n:Tooltip (en="Fixed interest rate- interest accrued on the Asset")
			@t24:i (align=R, inputBehaviour=F, maxLength=11, sysNumber=25.0, core=true, t24Name=AS.FIXED.RATE)
		/* Specifies the treasury interest rate for the Asset leg.

Once the contract has been drawn down, ie. the contract status is "CUR", this field cannot be changed. However, the interest rate can be changed at any time via the definition of a Rate Reset ("RR") schedule.

Validation Rules

1-11 numeric characters. (Optional Input).
Input is prohibited if data is present in the fields AS.RATE.KEY and AS.SPREAD and TREASURY.CUSTOMER is not set as NO..
A rate must be present in this field if the fields AS.RATE.KEY and AS.SPREAD are null.
Field will be no-input once the contract has reached the Interest effective date, ie. contract status = "CUR". */
		AS_TRSY_FIXED_RATE : mml:Integer
			@i18n:Label (en="Treasury Fixed Rate")
			@i18n:Tooltip (en="Treasury interest rate for the Asset leg")
			@t24:i (align=R, inputBehaviour=F, maxLength=11, sysNumber=26.0, core=true, t24Name=AS.TRSY.FIXED.RATE)
		/* Contains the key to the MARKET.RATE.TEXT table which indicates the underlying market rate that is to be applied if the asset side of the swap contract is floating.
This is only a mechanism to indicate the asset side of the swap contract is floating.  The user is required to give a basis rate for the calculation of interest for the interest period.
Validation Rules
1-3 numeric characters. Input is mandatory if the interest rate type is floating, ie. AS.FIXED.RATE has not been defined, otherwise it is not allowed. (Default value is blank.)
The key entered in this field must be a valid key on the MARKET.RATE.TEXT table. */
		AS_RATE_KEY -> ST_RateParameters:MARKET_RATE_TEXT
			@i18n:Label (en="Floating Interest")
			@i18n:Tooltip (en="Underlying market rate - asset side floating")
			@t24:i (align=L, inputBehaviour=I, maxLength=6, sysNumber=27.0, core=true, businessType=A, t24Name=AS.RATE.KEY)
		/* Indicates the spread (also known as the margin) which will be applied to the percentage interest rate related to the INTEREST KEY.
Note:   T24 allows the user to define, on Floating rate         contracts, a spread which can be added to or subtracted         from the percentage interest rate related to the         INTEREST KEY.  The subtraction of the INTEREST         SPREAD from the INTEREST KEY must be specifically         indicated by the insertion of the '-' (minus) sign in         front of the INTEREST SPREAD.
Validation Rules
Upto 11 numeric characters plus a decimal point. A minus sign has to be placed in front if the spread is to be deducted from the INTEREST KEY.  Input is optional when a rate key has been define, ie. AS.RATE.KEY, otherwise input is not allowed.  (Default value = null)
Input in this field is only allowed for contracts when a rate key has been specified, ie. AS.RATE.KEY is not null. */
		AS_SPREAD : mml:Integer
			@i18n:Label (en=Spread)
			@i18n:Tooltip (en="Spread, interest rate related to interest key")
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=28.0, core=true, t24Name=AS.SPREAD)
		/* Specifies the maximum interest rate that can be specified on the Asset leg. (Optional input.  Can be overridden).
Validation Rules
1-11 type "R" (Rate) numeric characters plus a decimal point. */
		AS_CAP_RATE : mml:Integer
			@i18n:Label (en="Asset Capt Rate")
			@i18n:Tooltip (en="Maximum interest rate can be for the Asset leg")
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=29.0, core=true, t24Name=AS.CAP.RATE)
		/* Specifies the minimum interest rate that can be specified on the Asset leg. (Optional input.  Can be overridden).
Validation Rules
1-11 type "R" (Rate) numeric characters plus a decimal point. */
		AS_FLOOR_RATE : mml:Integer
			@i18n:Label (en="Asset Floor Rate")
			@i18n:Tooltip (en="Minimum interest rate can be for the Asset leg")
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=30.0, core=true, t24Name=AS.FLOOR.RATE)
		/* The date of a call option (if appropriate).
Validation Rules
Standard date format (11 characters). (Optional input). */
		AS_OPTION_DATE : T24BusinessTypes:D
			@i18n:Label (en="Asset option Date")
			@i18n:Tooltip (en="The date of a call option")
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=31.0, core=true, t24Name=AS.OPTION.DATE)
		/* Definition of the number of days notice for the call option on the Asset leg.
Validation Rules
1-3 Numeric characters. (Optional input). */
		AS_OPTION_NOTICE : mml:Integer
			@i18n:Label (en="Asset option Notice")
			@i18n:Tooltip (en="Number of days notice for call option on Asset leg")
			@t24:i (align=R, inputBehaviour=I, maxLength=3, sysNumber=32.0, core=true, t24Name=AS.OPTION.NOTICE)
		/* Specifies the interest rate for the current interest period of the Swap asset leg.
This field contains the current rate of interest to be used when accruing interest on the outstanding balance of the asset side of the contract.
This field is a no-input field and is defaulted via :
o  The fixed interest rate (AS.FIXED.RATE) or,    o  The combined/calculated floating interest rate using       AS.RATE.KEY and AS.SPREAD
This rate can only be changed by defining and processing a Rate Reset schedule (type "RR") on the contract.
Validation Rules
1-11 numeric characters. (No-input field).
If the current rate is not greater than zero, an override message is set. */
		AS_CURRENT_RATE : mml:Integer
			@i18n:Label (en="Asset current Rate")
			@i18n:Tooltip (en="Interest rate for the current period - asset leg")
			@t24:i (align=R, inputBehaviour=N, maxLength=11, sysNumber=33.0, core=true, t24Name=AS.CURRENT.RATE)
		/* Specifies the treasury interest rate for the current interest period of the Swap asset leg.

This field along with AS.CURRENT.RATE forms the market interest rate  to be used when accruing interest on the outstanding balance of the asset side of the contract.

This field is a no-input field and is defaulted via :
The treasury interest rate (AS.TRSY.FIXED.RATE)

This rate can only be changed by defining and processing a Rate Reset schedule (type "RR") on the contract.

Validation Rules
1-11 numeric characters. (No-input field). */
		AS_CUR_TRSRY_RATE : mml:Integer
			@i18n:Label (en="Asset Current Treasury Rate")
			@i18n:Tooltip (en="Treasury rate for current period-asset leg")
			@t24:i (align=R, inputBehaviour=N, maxLength=11, sysNumber=34.0, core=true, t24Name=AS.CUR.TRSRY.RATE)
		/* Specifies whether the current interest amount payments on the asset leg are of a fixed or floating amount.
This field is a no-input field and is set to either "Y" for fixed interest amounts, and "NO" (default) for floating interest amounts.
This field is determined by the interest payment schedules ("IP") defined on the Asset side of the contract.
o  If the IP schedule (AS.TYPE) has an amount specified       (AS.AMOUNT) then fixed interest is "Y".    o  If the IP schedule (AS.TYPE) does not have an amount       specified (AS.AMOUNT) then fixed interest is "NO".
Validation Rules
1-2 Alpha characters. "Y" signifies a fixed interest amount for the current period. "NO" signifies a floating interest amount for the current period. (No-input field.  Default = "NO"). */
		AS_FIXED_INTEREST : SW_Contract:SWAP__AS_FIXED_INTEREST
			@i18n:Label (en="Asset Fixed Interest")
			@i18n:Tooltip (en="Current interest on asset leg-fixed or floating ")
			@t24:i (align=L, inputBehaviour=N, maxLength=2, sysNumber=35.0, core=true, t24Name=AS.FIXED.INTEREST)
		/* Defines the Interest Day Basis which indicates the calculation method to be used for interest amount calculations on the asset leg.
Input can be any valid basis type defined on the INTEREST.BASIS table.
Eg. Basis Calculation method signified ===== ============================
A 360/360 A2 30/360 B 366/360 C Act/Act (ISDA) C2 Act/Act (AFB) D 360/366 E 366/365 E1 365/365 F 360/365 F2 30/365 S SPECIAL
Validation Rules
1-10 Alphanumeric characters. (Mandatory input). The Basis type entered must be a valid key on the INTEREST.BASIS table. Defaulted to the Interest Day Basis specified on the CURRENCY file for the Asset currency (AS.CURRENCY). For Basis type 'S' the INTEREST amount will be Mandatory. Also FIXED.RATE and RATE.KEY are not allowed. */
		AS_BASIS -> ST_RateParameters:INTEREST_BASIS
			@i18n:Label (en="Interest Basis")
			@i18n:Tooltip (en="Interest Day Basis")
			@t24:i (align=L, inputBehaviour=I, maxLength=2, sysNumber=36.0, core=true, businessType=A, t24Name=AS.BASIS)
		/* Defines the Day Convention to follow.
Based on the Day Convention (AS.DAY.CONVENTION) and the Business Centres (AS.BUS.CENTRES) the process date of the Asset leg schedules are determined.
Valid Day Conventions are as follows :
o  null       the process date is the date of the schedule.
o  FOLLOWING  the process date is the next business day                 after the date of the schedule.
o  MODIFIED   the process date is the next business day                 after the date of the schedule providing this                 date does not fall into the next calendar month.                 In which case the process date is the first                 preceding business day.
o  PRECEDING  the process date is the first preceding business                 day.
Validation Rules
1-10 Alpha-numeric characters. (Optional input.  Default = null).
Valid Day Conventions are either "null", "FOLLOWING", "PRECEDING", or "MODIFIED". */
		AS_DAY_CONVENTION : SW_Contract:SWAP__AS_DAY_CONVENTION
			@i18n:Label (en=Convention)
			@i18n:Tooltip (en="Defines the Day Convention to follow")
			@t24:i (align=L, inputBehaviour=I, maxLength=10, sysNumber=37.0, core=true, t24Name=AS.DAY.CONVENTION)
		/* Defines the type of date adjustments that are to be followed on the Asset leg.
Depending on the date adjustment, the process date of the schedules and the interest effective dates are calculated.
Valid date adjustments and their effects are as follows :
DATE.ADJUSTMENT   ENTRY.DATE      EFFECTIVE.DATE   ===============   ==========      ==============
o  null           SCHEDULE.DATE   SCHEDULE.DATE   o  VALUE          PROCESS.DATE    SCHEDULE.DATE   o  PERIOD         PROCESS.DATE    PROCESS.DATE
Where,
ENTRY.DATE      is the process date of the schedule determined                 by the Date Adjustment specified                 (AS.DATE.ADJUSTMENT).
EFFECTIVE.DATE  is the end date of the current interest period                 determined by the Date Adjustment specified                 (AS.DATE.ADJUSTMENT).
SCHEDULE.DATE   is the date of the schedule as specified in the                 swap contract (AS.DATE.FREQ).
PROCESS.DATE    is the process date of the schedule determined by                 the Asset Day Convention (AS.DAY.CONVENTION).
Validation Rules
1-6 Alpha-numeric characters. (Optional input.  Default = null).
Must be set to either "null", "VALUE", or "PERIOD". */
		AS_DATE_ADJUSTMENT : SW_Contract:SWAP__AS_DATE_ADJUSTMENT
			@i18n:Label (en=Adjustment)
			@i18n:Tooltip (en="Type of date adjustments followed on the Asset leg")
			@t24:i (align=L, inputBehaviour=I, maxLength=6, sysNumber=38.0, core=true, t24Name=AS.DATE.ADJUSTMENT)
		AS_COUNTRY_RISK -> ST_Config:COUNTRY
			@i18n:Label (en="Asset Country Risk")
			@i18n:Tooltip (en="Indicates the country specific risk on asset side")
			@t24:i (align=L, inputBehaviour=I, maxLength=9, sysNumber=39.0, core=true, businessType=S, t24Name=AS.COUNTRY.RISK)
		AS_BUS_CENTRES -> byValue SW_Contract:SWAP__AS_BUS_CENTRES *
		AS_TYPE -> byValue SW_Contract:SWAP__AS_TYPE *
		/* Indicates the currency of the liability side of the swap contract.
This field indicates the currency of the liability side of the swap contract and will be the same as the currency of the various accounts where the accounting movements in respect of the contract will be debited or credited.
Validation Rules
3 type SSS (uppercase alpha) characters. or     1-3 numeric character currency code. (Optional input; Default = Asset currency).
The currency code must exist on the CURRENCY table.  Any existing currency code will be accepted and the name redisplayed for checking.
Defaults to the currency defined on the Asset leg (AS.CURRENCY).
Once the contract has been authorised this field cannot be changed. */
		LB_CURRENCY -> FIN_CurrencyConfig:CURRENCY
			@i18n:Label (en=Currency)
			@i18n:Tooltip (en="Currency in the asset side of the swap contract.")
			@t24:i (align=L, inputBehaviour=F, maxLength=3, sysNumber=52.0, core=true, businessType=CCY, t24Name=LB.CURRENCY)
		/* The Principal amount of the Liability side of the swap contract which will always be expressed in the currency defined in the previous field.
In order to facilitate the efficient entry of the amount, an input feature has been provided which allows for the suppression of trailing zeros.
In English Language text this is accomplished by using a 'T' to indicate the thousands and an 'M' to indicate the millions. Other languages will require the provision of translation values.
E.g.       INPUT              VALUE
1T               1,000.00              2M               2,000,000.00
Validation Rules
1-19 type AMT (standard amount format) characters plus a decimal point. (Mandatory input) The amount must be greater than Zero.
Defaults to the principal amount defined on the Asset leg (AS.PRINCIPAL). */
		LB_PRINCIPAL : T24BusinessTypes:AMT
			@i18n:Label (en=Principal)
			@i18n:Tooltip (en="Principal amount in Asset side of swap contract")
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=53.0, core=true, t24Name=LB.PRINCIPAL, typeModifiers="52")
		/* Identifies the start date of the first interest period and therefore the true Value date of the Liability leg.
It is the date on which interest accruals commence on the outstanding balance of the contract.
This date can be entered as either the current date (today) or a date in the past (less than the contract value date).  It cannot be greater than the contract value date.
If forward dated, ie. both the value date and interest effective date are forward, then the contract status will be null until the effective date is reached, at which time, the principal amount will be drawn down and the contract status changed to "CUR".
An input of a null value will result in the date being defaulted to the contract value date.
If backdated or 'today' dated then the contract will be drawn down, put on-line, and the contract status set to "CUR".
If backdated into a prior month, interest will be accrued on-line up to the end of the previous month.
Input of a non working day will require an override.
Validation Rules
Standard Date format.
Defaults to the contract Value Date.
Cannot be greater than the contract Value Date.
Input is prohibited was the contract status has been set to "CUR" (Current).
An override will be required if this date is a non-working day. */
		LB_INT_EFFECTIVE : T24BusinessTypes:D
			@i18n:Label (en="Effective Date")
			@i18n:Tooltip (en="Start date of the first liability interest period")
			@t24:i (align=R, inputBehaviour=F, maxLength=11, sysNumber=54.0, core=true, t24Name=LB.INT.EFFECTIVE)
		/* Specifies the fixed interest rate at which interest is to be accrued on the Liability leg.
Once the contract has been drawn down, ie. the contract status is "CUR", this field cannot be changed.  However, the interest rate can be changed at any time via the definition of a Rate Reset ("RR") schedule.
Validation Rules
1-11 numeric characters. (Optional Input).
Input is prohibited if data is present in the fields AS.RATE.KEY and AS.SPREAD.
A rate must be present in this field if the fields AS.RATE.KEY and AS.SPREAD are null.
Field will be no-input once the contract has reached the Interest effective date, ie. contract status = "CUR". */
		LB_FIXED_RATE : mml:Integer
			@i18n:Label (en="Fixed Rate")
			@i18n:Tooltip (en="Fixed interest rate- interest accrued on the Liability")
			@t24:i (align=R, inputBehaviour=F, maxLength=11, sysNumber=55.0, core=true, t24Name=LB.FIXED.RATE)
		/* Specifies the treasury interest rate for the Liability leg.

Once the contract has been drawn down, ie. the contract status is "CUR", this field cannot be changed. However, the interest rate can be changed at any time via the definition of a Rate Reset ("RR") schedule.

Validation Rules

1-11 numeric characters. (Optional Input).
Input is prohibited if data is present in the fields LB.RATE.KEY and LB.SPREAD and TREASURY.CUSTOMER is not set as NO..
A rate must be present in this field if the fields LB.RATE.KEY and LB.SPREAD are null.
Field will be no-input once the contract has reached the Interest effective date, ie. contract status = "CUR". */
		LB_TRSY_FIXED_RATE : mml:Integer
			@i18n:Label (en="Treasury Rate")
			@i18n:Tooltip (en="Treasury interest rate for the Asset leg")
			@t24:i (align=R, inputBehaviour=F, maxLength=11, sysNumber=56.0, core=true, t24Name=LB.TRSY.FIXED.RATE)
		/* Contains the key to the MARKET.RATE.TEXT table which indicates the underlying market rate that is to be applied if the liability side of the swap contract is floating.
This is only a mechanism to indicate the liability side of the swap contract is floating.  The user is required to give a basis rate for the calculation of interest for the interest period.
Validation Rules
1-3 numeric characters. Input is mandatory if the interest rate type is floating, ie. LB.FIXED.RATE has not been defined, otherwise it is not allowed. (Default value is blank.)
The key entered in this field must be a valid key on the MARKET.RATE.TEXT table. */
		LB_RATE_KEY -> ST_RateParameters:MARKET_RATE_TEXT
			@i18n:Label (en="Floating Interest")
			@i18n:Tooltip (en="Underlying market rate - liability side floating")
			@t24:i (align=L, inputBehaviour=I, maxLength=6, sysNumber=57.0, core=true, businessType=A, t24Name=LB.RATE.KEY)
		/* Indicates the spread (also known as the margin) which will be applied to the percentage interest rate related to the INTEREST KEY.
Note : T24 allows the user to define, on Floating rate contracts, a spread which can be added to or subtracted from the percentage interest rate related to the INTEREST KEY. The subtraction of the INTEREST SPREAD from the INTEREST KEY must be specifically indicated by the insertion of the '-' (minus) sign in front of the INTEREST SPREAD.
Validation Rules

Upto 11 numeric characters plus a decimal point. A minus         sign has to be placed in front if the spread is to be         deducted from the INTEREST KEY. Input is optional when a         rate key has been define, ie. LB.RATE.KEY, otherwise         input is not allowed. (Default value = null)
Input in this field is only allowed for contracts when a         rate key has been specified, ie. LB.RATE.KEY is not null. */
		LB_SPREAD : mml:Integer
			@i18n:Label (en=Spread)
			@i18n:Tooltip (en="Spread, interest rate related to interest key")
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=58.0, core=true, t24Name=LB.SPREAD)
		/* Specifies the maximum interest rate that can be specified on the Liability leg. (Optional input.  Can be overridden).
Validation Rules
1-11 type "R" (Rate) numeric characters plus a decimal point. */
		LB_CAP_RATE : mml:Integer
			@i18n:Label (en="Liability Cap Rate")
			@i18n:Tooltip (en="Maximum interest rate on the Liability leg")
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=59.0, core=true, t24Name=LB.CAP.RATE)
		/* Specifies the minimum interest rate that can be specified on the Liability leg. (Optional input.  Can be overridden).
Validation Rules
1-11 type "R" (Rate) numeric characters plus a decimal point. */
		LB_FLOOR_RATE : mml:Integer
			@i18n:Label (en="Liability Floor Rate")
			@i18n:Tooltip (en="Minimum interest rate on the Liability leg")
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=60.0, core=true, t24Name=LB.FLOOR.RATE)
		/* The date of a call option (if appropriate).
Validation Rules
Standard date format (11 characters). (Optional Input). */
		LB_OPTION_DATE : T24BusinessTypes:D
			@i18n:Label (en="Liability Option Date")
			@i18n:Tooltip (en="The date of a call option")
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=61.0, core=true, t24Name=LB.OPTION.DATE)
		/* Definition of the number of days notice for the call option on the Liability leg.
Validation Rules
1-3 Numeric characters. (Optional input). */
		LB_OPTION_NOTICE : mml:Integer
			@i18n:Label (en="Liability Option Notice")
			@i18n:Tooltip (en="Number of days notice-call option on Liability leg")
			@t24:i (align=R, inputBehaviour=I, maxLength=3, sysNumber=62.0, core=true, t24Name=LB.OPTION.NOTICE)
		/* Specifies the interest rate for the current interest period of the Swap liability leg.
This field contains the current rate of interest to be used when accruing interest on the outstanding balance of the liability side of the contract.
This field is a no-input field and is defaulted via :
o  The fixed interest rate (LB.FIXED.RATE) or,    o  The combined/calculated floating interest rate using       LB.RATE.KEY and LB.SPREAD
This rate can only be changed by defining and processing a Rate Reset schedule (type "RR") on the contract.

Validation Rules
1-11 numeric characters. (No-input field).
If the current rate is not greater than zero, an override message is set. */
		LB_CURRENT_RATE : mml:Integer
			@i18n:Label (en="Liability Current Rate")
			@i18n:Tooltip (en="Interest rate for the current period-liability leg")
			@t24:i (align=R, inputBehaviour=N, maxLength=11, sysNumber=63.0, core=true, t24Name=LB.CURRENT.RATE)
		/* Specifies the treasury interest rate for the current interest period of the Swap liability leg.

This field along with LB.CURRENT.RATE forms the market interest rate  to be used when accruing interest on the outstanding balance of the liability side of the contract.

This field is a no-input field and is defaulted via :

The treasury interest rate (LB.TRSY.FIXED.RATE)

This rate can only be changed by defining and processing a Rate Reset schedule (type "RR") on the contract.

Validation Rules
1-11 numeric characters. (No-input field). */
		LB_CUR_TRSRY_RATE : mml:Integer
			@i18n:Label (en="Liability Current Treasury Rate")
			@i18n:Tooltip (en="Treasury rate for current period-Liability leg")
			@t24:i (align=R, inputBehaviour=N, maxLength=11, sysNumber=64.0, core=true, t24Name=LB.CUR.TRSRY.RATE)
		/* Specifies whether the current interest amount payments on the liability leg are of a fixed or floating amount.
This field is a no-input field and is set to either "Y" for fixed interest amounts, and "NO" (default) for floating interest amounts.
This field is determined by the interest payment schedules ("IP") defined on the Asset side of the contract.
o  If the IP schedule (LB.TYPE) has an amount specified       (LB.AMOUNT) then fixed interest is "Y".    o  If the IP schedule (LB.TYPE) does not have an amount       specified (LB.AMOUNT) then fixed interest is "NO".
Validation Rules
1-2 Alpha characters. "Y" signifies a fixed interest amount for the current period. "NO" signifies a floating interest amount for the current period. (No-input field.  Default = "NO"). */
		LB_FIXED_INTEREST : SW_Contract:SWAP__LB_FIXED_INTEREST
			@i18n:Label (en="Liability Fixed Interest")
			@i18n:Tooltip (en="Current interest on liability leg-fixed/floating")
			@t24:i (align=L, inputBehaviour=N, maxLength=2, sysNumber=65.0, core=true, t24Name=LB.FIXED.INTEREST)
		/* Defines the Interest Day Basis which indicates the calculation method to be used for interest amount calculations on the liability leg.
Input can be any valid basis type defined on the INTEREST.BASIS table.
Eg. Basis Calculation method signified ===== ============================
A 360/360 A2 30/360 B 366/360 C Act/Act (ISDA) C2 Act/Act (AFB) 366/366 for leap years D 360/366 E 366/365 E1 365/365 F 360/365 F2 30/365 S SPECIAL
Validation Rules
1-10 Alphanumeric characters. (Mandatory input). The Basis type entered must be a valid key on the INTEREST.BASIS table. Defaulted to the Interest Day Basis specified on the CURRENCY file for the Liability currency (LB.CURRENCY). For Basis type 'S' the INTEREST amount will be Mandatory. Also FIXED.RATE and RATE.KEY are not allowed. */
		LB_BASIS -> ST_RateParameters:INTEREST_BASIS
			@i18n:Label (en="Interest Basis")
			@i18n:Tooltip (en="Interest Day Basis")
			@t24:i (align=L, inputBehaviour=I, maxLength=2, sysNumber=66.0, core=true, businessType=A, t24Name=LB.BASIS)
		/* Defines the Day Convention to follow.
Based on the Day Convention (LB.DAY.CONVENTION) and the Business Centres (LB.BUS.CENTRES) the process date of the Liability leg schedules are determined.
Valid Day Conventions are as follows :
o  null       the process date is the date of the schedule.
o  FOLLOWING  the process date is the next business day                 after the date of the schedule.
o  MODIFIED   the process date is the next business day                 after the date of the schedule providing this                 date does not fall into the next calendar month.                 In which case the process date is the first                 preceding business day.
o  PRECEDING  the process date is the first preceding business                 day.
Validation Rules
1-10 Alpha-numeric characters. (Optional input.  Default = null).
Valid Day Conventions are either "null", "FOLLOWING", "PRECEDING", or "MODIFIED". */
		LB_DAY_CONVENTION : SW_Contract:SWAP__LB_DAY_CONVENTION
			@i18n:Label (en=Convention)
			@i18n:Tooltip (en="Defines the Day Convention to follow")
			@t24:i (align=L, inputBehaviour=I, maxLength=10, sysNumber=67.0, core=true, t24Name=LB.DAY.CONVENTION)
		/* Defines the type of date adjustments that are to be followed on the Liability leg.
Depending on the date adjustment, the process date of the schedules and the interest effective dates are calculated.
Valid date adjustments and their effects are as follows :
DATE.ADJUSTMENT   ENTRY.DATE      EFFECTIVE.DATE   ===============   ==========      ==============
o  null           SCHEDULE.DATE   SCHEDULE.DATE   o  VALUE          PROCESS.DATE    SCHEDULE.DATE   o  PERIOD         PROCESS.DATE    PROCESS.DATE
Where,
ENTRY.DATE      is the process date of the schedule determined                 by the Date Adjustment specified                 (LB.DATE.ADJUSTMENT).
EFFECTIVE.DATE  is the end date of the current interest period                 determined by the Date Adjustment specified                 (LB.DATE.ADJUSTMENT).
SCHEDULE.DATE   is the date of the schedule as specified in the                 swap contract (LB.DATE.FREQ).
PROCESS.DATE    is the process date of the schedule determined by                 the Asset Day Convention (LB.DAY.CONVENTION).
Validation Rules
1-6 Alpha-numeric characters. (Optional input.  Default = null).
Must be set to either "null", "VALUE", or "PERIOD". */
		LB_DATE_ADJUSTMENT : SW_Contract:SWAP__LB_DATE_ADJUSTMENT
			@i18n:Label (en=Adjustment)
			@i18n:Tooltip (en="Type of date adjustments followed on the Asset leg")
			@t24:i (align=L, inputBehaviour=I, maxLength=6, sysNumber=68.0, core=true, t24Name=LB.DATE.ADJUSTMENT)
		/* Validation Rules */
		LB_COUNTRY_RISK -> ST_Config:COUNTRY
			@i18n:Label (en="Liability Country Risk")
			@i18n:Tooltip (en="Country specific risk on liability side")
			@t24:i (align=L, inputBehaviour=I, maxLength=9, sysNumber=69.0, core=true, businessType=S, t24Name=LB.COUNTRY.RISK)
		LB_BUS_CENTRES -> byValue SW_Contract:SWAP__LB_BUS_CENTRES *
		LB_TYPE -> byValue SW_Contract:SWAP__LB_TYPE *
		SET_CURRENCY -> byValue SW_Contract:SWAP__SET_CURRENCY *
		/* Indicates the Customer code of the broker that applies to the contract.
Validation Rules
1-10 type CUS Numeric characters. (Optional input).
Must be a valid definition on the CUSTOMER file.
BROKERAGE.PAYABLE must be previously defined on SWAP.PARAMETER.
NOCHANGE field. */
		BROKER_CODE -> ST_Customer:CUSTOMER
			@i18n:Label (en="Broker Code")
			@i18n:Tooltip (en="Indicates the broker")
			@t24:i (align=R, inputBehaviour=F, maxLength=10, sysNumber=92.0, core=true, businessType=CUS, t24Name=BROKER.CODE)
		/* Indicates whether the asset or the liability side will be used in the brokerage calculation.
Validation Rules
Valid input is either ASSET or LIABILITY. (Optional input field. Default to the fixed rate sied.)
Mandatory input if BROKER.CODE is specified.
NOCHANGE field. */
		BROKERAGE_BASE : SW_Contract:SWAP__BROKERAGE_BASE
			@i18n:Label (en="Brokerage Base")
			@i18n:Tooltip (en="Brokerage Base")
			@t24:i (align=L, inputBehaviour=F, maxLength=9, sysNumber=93.0, core=true, t24Name=BROKERAGE.BASE)
		/* Contains the amount of brokerage to be paid to the broker defined above.  The brokerage will be expressed in either the asset currency or the liability currency depending on the value in the BROKERAGE.BASE field.
If no value is entered here but a broker code is defined above, then the system will attempt to calculate a default brokerage amount based on the conditions set up on the BROKER file.  If an amount is calculated succesfully then it will be defaulted in this field.

Validation Rules
1-19 standard amount format. (Optional input.  Default calculated using the BROKER file.)
Mandatory input if BROKER.CODE is specified.
NOCHANGE field. */
		BROKERAGE : T24BusinessTypes:AMT
			@i18n:Label (en=Brokerage)
			@t24:i (align=R, inputBehaviour=F, maxLength=19, sysNumber=94.0, core=true, t24Name=BROKERAGE, typeModifiers="22")
		/* Indicates the currency code of the associated broker.
Settlement of the brokerage can be made in any currency, ie. different than the currencies defined for each of the legs of the contract, providing that it has been defined in the brokerage conditions on the BROKER table.
Validation Rules
3 Type CCY character Currency code. (Input only allowed when brokerage has been defined).
Must be a valid definition on the CURRENCY file.
Input is not allowed in this field when the broker has not been specified.
NOCHANGE field. */
		BROKER_CURRENCY -> FIN_CurrencyConfig:CURRENCY
			@i18n:Label (en="Broker Currency")
			@i18n:Tooltip (en="Broker Currency")
			@t24:i (align=L, inputBehaviour=F, maxLength=3, sysNumber=95.0, core=true, businessType=CCY, t24Name=BROKER.CURRENCY)
		/* Contains the amount of brokerage to be credited to the broker account specified in the broker currency defined above.
Validation Rules
1 to 19 Type AMT Numeric characters. Standard amount format for currency. (Optional input).
Input is not allowed in this field when the broker code has not been entered.
NOCHANGE field. */
		BROKER_AMOUNT : T24BusinessTypes:AMT
			@i18n:Label (en="Brokerage Amount")
			@i18n:Tooltip (en="Specifies the brokerage amount")
			@t24:i (align=R, inputBehaviour=F, maxLength=19, sysNumber=96.0, core=true, t24Name=BROKER.AMOUNT, typeModifiers="95")
		/* Field to control whether Swift payment messages are to be sent or not.
Validation Rules
1 - 2 Alphanumeric characters. (Optional input.  Default = "Y").
Valid field entries are, "Y" to send payment messages and "NO" to signify that no messages are to be sent. */
		SEND_PAYMENT : SW_Contract:SWAP__SEND_PAYMENT
			@i18n:Label (en="Send Payment")
			@i18n:Tooltip (en="Send payment - Y/N ?")
			@t24:i (align=L, inputBehaviour=I, maxLength=2, sysNumber=97.0, core=true, t24Name=SEND.PAYMENT)
		/* Field to signify whether confirmations are to be sent or not.
o  "Y"   Send advices and confirmations   o  "NO"  Do not send any advices or confirmations.
Swap confirmations are message types 360 and 361. All other non payment message types are treated as ADVICES and can be suppressed by using the field SEND.ADVICE.
Validation Rules
1-2 Alpha-numeric characters. (Optional input.  Default = "Y").
Field must be "Y" or "NO". */
		SEND_CONFIRMATION : SW_Contract:SWAP__SEND_CONFIRMATION
			@i18n:Label (en="Send Confirmation")
			@i18n:Tooltip (en="Send confirmation - Y/N ?")
			@t24:i (align=L, inputBehaviour=I, maxLength=2, sysNumber=98.0, core=true, t24Name=SEND.CONFIRMATION)
		/* Specifies the number of working days, in advance of the Process Date, that an advice/confirmation is to be sent by the Delivery application.
Validation Rules
1-2 Numeric characters. (Optional input.  Default = null.) */
		DAYS_DELIVERY : mml:Integer
			@i18n:Label (en="Delivery Days")
			@t24:i (align=R, inputBehaviour=I, maxLength=2, sysNumber=99.0, core=true, t24Name=DAYS.DELIVERY)
		/* This field is used by the confirmation facility (function code "V") to store automatically the user identification, date, and time the customer note, confirmation, was received and reconciled with the swap contract.
Values :
o  User + DateTime of confirmation   o  "ONESIDED"   o  NULL - awaiting confirmation.
Once an event has been confirmed, this field must be blanked out (a NULL input into this field) before the next event may be confirmed.
Validation Rules
1-15 Alphanumeric characters. (Optional Input).
System generated field or "ONESIDED" to signify . */
		CONFIRM_BY_CUST : SW_Contract:SWAP__CONFIRM_BY_CUST
			@i18n:Label (en="Confirmation By Customer")
			@t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=100.0, core=true, t24Name=CONFIRM.BY.CUST)
		/* This field is used by the confirmation facility (function code "V") to store automatically  the user identification, date and time the broker note, confirmation, was received and reconciled with the swap contract.
Values :
o  User + DateTime of confirmation   o  "ONESIDED"   o  NULL - awaiting confirmation.
Once an event has been confirmed, this field must be blanked out (a NULL input into this field) before the next event may be confirmed.
Validation Rules
1-15 Alphanumeric characters. (Optional input).
System generated field or "ONESIDED" to signify . */
		CONFIRM_BY_BROKER : SW_Contract:SWAP__CONFIRM_BY_BROKER
			@i18n:Label (en="Confirmation By Broker")
			@t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=101.0, core=true, t24Name=CONFIRM.BY.BROKER)
		/* No-input field that identifies the current value of the Swap contract.
Validation Rules
1 to 19 Type AMT characters. Standard amount field. (No-input.  Default = Oustanding value of contract). */
		FWD_REVAL_AMOUNT : T24BusinessTypes:AMT
			@i18n:Label (en="Forward Revaluation Amount")
			@i18n:Tooltip (en="Field that identifies current value of the Swap")
			@t24:i (align=R, inputBehaviour=N, maxLength=19, sysNumber=102.0, core=true, t24Name=FWD.REVAL.AMOUNT)
		/* Defines the current status of the Swap contract.
The following values are possible :
o  null  contract either unauthorised, or has not yet            reached the contract Value Date, ie. the contract            is forward.   o  CUR   contract has reached the contract Value Date, ie.            the contract is current ("live").   o  MAT   contract has Matured.
This no-input field is automatically updated.
Validation Rules
1-3 Alpha-numeric characters. (No-input field.  Default = the status of the contract.). */
		CONTRACT_STATUS : SW_Contract:SWAP__CONTRACT_STATUS
			@i18n:Label (en="Contract Status")
			@t24:i (align=L, inputBehaviour=N, maxLength=3, sysNumber=103.0, core=true, t24Name=CONTRACT.STATUS)
		/* Defines the current status of the asset leg of the Swap contract.
The following values are possible :
o  null  leg either unauthorised, or has not yet            reached the Interest Effective Date, ie. the leg            is forward.   o  CUR   leg has reached the Interest Effective Date, ie.            the leg is current ("live").   o  MAT   leg has Matured.
This no-input field is automatically updated.
Validation Rules
1-3 Alpha-numeric characters. (No-input field.  Default = the status of the contract.). */
		ASSET_STATUS : SW_Contract:SWAP__ASSET_STATUS
			@i18n:Label (en="Asset Status")
			@i18n:Tooltip (en="Current status of asset leg of the Swap contract")
			@t24:i (align=L, inputBehaviour=N, maxLength=3, sysNumber=104.0, core=true, t24Name=ASSET.STATUS)
		/* Defines the current status of the liability leg of the Swap contract.
The following values are possible :
o  null  leg either unauthorised, or has not yet            reached the Interest Effective Date, ie. the leg            is forward.   o  CUR   leg has reached the Interest Effective Date, ie.            the leg is current ("live").   o  MAT   leg has Matured.
This no-input field is automatically updated.
Validation Rules
1-3 Alpha-numeric characters. (No-input field.  Default = the status of the contract.). */
		LIABILITY_STATUS : SW_Contract:SWAP__LIABILITY_STATUS
			@i18n:Label (en="Liability Status")
			@i18n:Tooltip (en="Current status of liability leg of the Swap")
			@t24:i (align=L, inputBehaviour=N, maxLength=3, sysNumber=105.0, core=true, t24Name=LIABILITY.STATUS)
		ACTIVITY -> byValue SW_Contract:SWAP__ACTIVITY *
		/* Signifies the amount utilised on the associated Limit.
Validation Rules
1-19 Type AMT Numeric characters. (No-input field.  Default = Amount utilised on Limit). */
		LIMIT_AMOUNT : T24BusinessTypes:AMT
			@i18n:Label (en="Limit Amount")
			@i18n:Tooltip (en="It shows the limit amount")
			@t24:i (align=R, inputBehaviour=N, maxLength=19, sysNumber=115.0, core=true, t24Name=LIMIT.AMOUNT, typeModifiers="22")
		/* This field details whether the SWAP contract is to be treated as an ON balance sheet instrument or an OFF balance sheet instrument. This value is defaulted from the SWAP.TYPE of the swap contract.
When this field contains the value OFF the contract will undergo the currency revaluation processing. Each night each leg of the contract id revalued against the local currency if
1) The leg is not notional (ie the NOTIONAL field on the SWAP.BALANCES file is set to NO).   2) The currency of the leg is not the local currency.
And any difference between this value and the value as of last night is posted as unreleasied profit and loss to the category specified on the SWAP.PARAMETER record.
When this field contains the value of ON the contract will not be revalued.
Validation Rules
Must contain either ON or OFF, which is set from the SWAP.TYPE. Input is not allowed to this field. */
		BALANCE_SHEET : mml:Integer
			@i18n:Label (en="Balance Sheet")
			@t24:i (align=R, inputBehaviour=N, maxLength=3, sysNumber=116.0, core=true, t24Name=BALANCE.SHEET)
		/* Identifies the base currency of the contract and will be used to verify the exchange rate.
When the Base Currency is not specified and the transaction involves local Currency, the System will default to the local Currency provided that the Quotation code of the other Currency is NULL.  If this is not the case (the Quotation code of the other Currency is not equal to null) then the other Currency will become the default.
When the base Currency is not specified and the transaction involves two currencies where none is the Local Currency, it will default to the Currency which yields a cross-rate greater than unity (One).
Please refer to Forex for more details.
Validation Rules
3 characters currency code (uppercase alpha or numeric) - Type SSS (Optional input. See details for default.)
The currency entered must appear on the Currency table.
The currency must be the same as either the asset or liability currency.
Proper verification of the exchange rate will be performed by the system against the Exchange Rate values contained in the Currency table.
When the Swap contract involves the local currency and the quotation code on the Foreign currency is equal to or greater than '0' (zero), the base currency can only be the Foreign Currency.
Once authorised the Base currency cannot be amended. */
		BASE_CURRENCY -> FIN_CurrencyConfig:CURRENCY
			@i18n:Label (en="Base Currency")
			@i18n:Tooltip (en="Base Currency")
			@t24:i (align=L, inputBehaviour=F, maxLength=3, sysNumber=117.0, core=true, businessType=CCY, t24Name=BASE.CURRENCY)
		CONTACT_DETAILS -> byValue SW_Contract:SWAP__CONTACT_DETAILS *
		AS_INTEREST_DET -> byValue SW_Contract:SWAP__AS_INTEREST_DET *
		LB_INTEREST_DET -> byValue SW_Contract:SWAP__LB_INTEREST_DET *
		/* Field to signify whether advices are to be sent or not.
o  "Y"   Send advices and confirmations   o  "NO"  Do not send any advices or confirmations.
Swap confirmations are message types 360 and 361. All other non payment message types (100, 202, 210) are treated as ADVICES.
Validation Rules
1-2 Alpha-numeric characters. (Optional input.  Default = "Y").
Field must be "Y" or "NO". */
		SEND_ADVICE : SW_Contract:SWAP__SEND_ADVICE
			@i18n:Label (en="Send Advice")
			@i18n:Tooltip (en="Transmission of the advice")
			@t24:i (align=L, inputBehaviour=I, maxLength=2, sysNumber=121.0, core=true, t24Name=SEND.ADVICE)
		/* Specifies the currency of the FWD.REVAL.AMOUNT field.
Validation Rules
3 Type CCY currency code. (No input field.  Default = AS.CURRENCY) */
		FWD_REVAL_CCY : mml:Integer
			@i18n:Label (en="Forward Revaluation Currency")
			@i18n:Tooltip (en="Specifies the forward revaluation currency")
			@t24:i (align=R, inputBehaviour=N, maxLength=3, sysNumber=122.0, core=true, t24Name=FWD.REVAL.CCY)
		/* FWD.REVAL.AMOUNT local equivalent.
This field will be null if no p&l entry is raised.
Validation Rules
1-19 Type AMT numeric characters. (No input field) */
		FWD_REVAL_PL_LCY : T24BusinessTypes:AMT
			@i18n:Label (en="Forward Revaluation Profit Loss Local Currency")
			@i18n:Tooltip (en="Forward revaluation profit loss in local currency")
			@t24:i (align=R, inputBehaviour=N, maxLength=19, sysNumber=123.0, core=true, t24Name=FWD.REVAL.PL.LCY, typeModifiers="USD")
		/* This field identifies the manner in which overdue payments are handled. The default for all contracts is AUTOMATIC.
There are three options available:           AUTOMATIC           SEMI-AUTOMATIC           MANUAL
AUTOMATIC will liquidate across the settlement accounts on the contract.
MANUAL will create a PD contract instead.
SEMI-AUTOMATIC will only raise a PD contract if insufficient funds exist on the settlement account. For further details see the helptext in PD.PARAMETER where these settings are controlled from.
Validation Rules
If the PD product is not installed for the current company then the only allowed setting is AUTOMATIC.
Only AUTOMATIC allowed for liability contract types. */
		LIQUIDATION_MODE : T24BusinessTypes:DEF
			@i18n:Label (en="Liquidation Mode")
			@i18n:Tooltip (en="Indicates how overdue payments are handled.")
			@t24:i (align=L, inputBehaviour=I, maxLength=15, sysNumber=124.0, core=true, t24Name=LIQUIDATION.MODE)
		/* If the contract is overdue, then this field identifies the status of the underlying PAYMENT.DUE contract.
Validation Rules
No input allowed. */
		OVERDUE_STATUS : mml:Integer
			@i18n:Label (en="Overdue Status")
			@i18n:Tooltip (en="Overdue or aging status of the contract")
			@t24:i (align=R, inputBehaviour=E, maxLength=3, sysNumber=125.0, core=true, t24Name=OVERDUE.STATUS)
		/* This field specifies whether the deal is to be processed at the start of day or at the end of day.
Input Yes to make the swap mature or take value during the start of day procedure (
SW.START.OF.DAY
SW.END.OF.DAY
Limit exposures for the swap are also updated during the start of day procedure (
SYSTEM.LIMIT.SOD
SYSTEM.LIMIT.EOD
All accounting entries, payments, payment messages and limit updates for deals maturing or taking value are raised during the start of day procedure.
If this field is left blank, the value of
SOD.MAT
SWAP.TYPE
Validation Rules

Y or N or Blank */
		SOD_MAT : SW_Contract:SWAP__SOD_MAT
			@i18n:Label (en="Sod Mat")
			@t24:i (align=L, inputBehaviour=I, maxLength=3, sysNumber=126.0, core=true, t24Name=SOD.MAT)
		FLEX_PRIN_PAYMENT : SW_Contract:SWAP__FLEX_PRIN_PAYMENT
			@i18n:Label (en="Flexible Principal Payment")
			@t24:i (align=L, inputBehaviour=F, maxLength=3, sysNumber=127.0, core=true, t24Name=FLEX.PRIN.PAYMENT)
		/* This field accepts input of YES, NO or NULL
Validation Rules

Default value is NULL
No input field after first authorisation
Liquidation mode should be AUTOMATIC for this field to be set as YES */
		NEGATIVE_RATES : SW_Contract:SWAP__NEGATIVE_RATES
			@i18n:Label (en="Negative Rates")
			@i18n:Tooltip (en="Indicate whether interest rate is negative or not")
			@t24:i (align=L, inputBehaviour=F, maxLength=11, sysNumber=128.0, core=true, t24Name=NEGATIVE.RATES)
		EB_ACCRUAL_PARAM -> AC_Fees:EB_ACCRUAL_PARAM
			@t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=129.0, core=true, businessType=A, t24Name=EB.ACCRUAL.PARAM)
		NPV_PL_TODAY : T24BusinessTypes:AMT
			@i18n:Label (en="Net Present Value Profit Loss Today")
			@t24:i (align=R, inputBehaviour=N, maxLength=19, sysNumber=130.0, core=true, t24Name=NPV.PL.TODAY)
		MAT_PAYMENT_DATE : SW_Contract:SWAP__MAT_PAYMENT_DATE
			@i18n:Label (en="Maturity Payment Date")
			@i18n:Tooltip (en="Maturity Payment Date")
			@t24:i (align=L, inputBehaviour=F, maxLength=3, sysNumber=131.0, core=true, t24Name=MAT.PAYMENT.DATE)
		RESERVED_3 : mml:Integer
			@t24:i (align=R, inputBehaviour=N, maxLength=55, sysNumber=132.0, core=true, t24Name=RESERVED.3)
		RESERVED_2 : mml:Integer
			@t24:i (align=R, inputBehaviour=N, maxLength=55, sysNumber=133.0, core=true, t24Name=RESERVED.2)
		RESERVED_1 : mml:Integer
			@t24:i (align=R, inputBehaviour=N, maxLength=55, sysNumber=134.0, core=true, t24Name=RESERVED.1)
		ENTRY_IDS -> byValue SW_Contract:SWAP__ENTRY_IDS *
		OVERRIDE -> byValue SW_Contract:SWAP__OVERRIDE *
		RECORD_STATUS : T24BusinessTypes:STAT
			@i18n:Label (en="Record Status")
			@t24:i (align=L, inputBehaviour=N, maxLength=4, sysNumber=137.0, core=true, t24Name=RECORD.STATUS)
		CURR_NO : mml:Integer
			@i18n:Label (en="Curr No")
			@t24:i (align=R, inputBehaviour=N, maxLength=4, sysNumber=138.0, core=true, t24Name=CURR.NO)
		INPUTTER -> byValue SW_Contract:SWAP__INPUTTER *
		DATE_TIME -> byValue SW_Contract:SWAP__DATE_TIME *
		AUTHORISER : T24BusinessTypes:A
			@i18n:Label (en=Authoriser)
			@i18n:Tooltip (en="Indicates the User who Authorises the record")
			@t24:i (align=L, inputBehaviour=N, maxLength=40, sysNumber=141.0, core=true, t24Name=AUTHORISER)
		CO_CODE -> ST_CompanyCreation:COMPANY
			@i18n:Label (en=Company)
			@t24:i (align=L, inputBehaviour=N, maxLength=11, sysNumber=142.0, core=true, businessType=A, t24Name=CO.CODE, mask="R##-###-####")
		DEPT_CODE -> ST_Config:DEPT_ACCT_OFFICER
			@i18n:Label (en="Dept Code")
			@t24:i (align=R, inputBehaviour=N, maxLength=4, sysNumber=143.0, core=true, t24Name=DEPT.CODE)
		AUDITOR_CODE : T24BusinessTypes:A
			@i18n:Label (en="Auditor Code")
			@i18n:Tooltip (en="Auditor Code")
			@t24:i (align=L, inputBehaviour=E, maxLength=16, sysNumber=144.0, core=true, t24Name=AUDITOR.CODE)
		AUDIT_DATE_TIME : T24BusinessTypes:RELTIME
			@i18n:Label (en="Audit Date Time")
			@i18n:Tooltip (en="Date and time when record was audited")
			@t24:i (align=L, inputBehaviour=E, maxLength=15, sysNumber=145.0, core=true, t24Name=AUDIT.DATE.TIME, mask="RDD DD  DD ##:##")
		SECURITY_ID : T24BusinessTypes:SEC
			@i18n:Label (en=Security)
			@t24:i (align=L, inputBehaviour=I, maxLength=12, sysNumber=106.1, core=false, t24Name=SECURITY.ID)
		DISCOUNT_RATE : mml:Integer
			@i18n:Label (en="Discount Rate")
			@t24:i (align=L, inputBehaviour=N, maxLength=55, core=false, t24Name=DISCOUNT.RATE, genOperation="\"\"")
		CURRENCY : mml:Integer
			@i18n:Label (en=Ccy)
			@t24:i (align=L, inputBehaviour=N, maxLength=3, core=false, t24Name=CURRENCY, genOperation=AS.CURRENCY)
		SOURCE_ID : mml:Integer
			@i18n:Label (en="Source Id")
			@t24:i (align=R, inputBehaviour=N, maxLength=30, core=false, t24Name=SOURCE.ID, genOperation="@ID")
		RESIDENCE : mml:Integer
			@i18n:Label (en=Residence)
			@t24:i (align=R, inputBehaviour=N, maxLength=4, core=false, t24Name=RESIDENCE, genOperation="CUSTOMER.NO>CUSTOMER>RESIDENCE")
		SOURCE_FILE : mml:Integer
			@i18n:Label (en="Source File")
			@t24:i (align=L, inputBehaviour=N, maxLength=4, core=false, t24Name=SOURCE.FILE, genOperation="\"SWAP\"")
		PRODCAT : mml:Integer
			@i18n:Label (en="Deal Type")
			@t24:i (align=R, inputBehaviour=N, maxLength=6, core=false, t24Name=PRODCAT, genOperation=PRODUCT.CATEGORY)
		PRODCCY : mml:Integer
			@i18n:Label (en="Product Currency")
			@t24:i (align=L, inputBehaviour=N, maxLength=1, core=false, t24Name=PRODCCY, genOperation="\"\"")
		CUSTOMER_NO : mml:Integer
			@i18n:Label (en="Customer No")
			@i18n:Tooltip (en="Identifies the Customer for the transaction.")
			@t24:i (align=R, inputBehaviour=N, maxLength=10, core=false, t24Name=CUSTOMER.NO, genOperation=CUSTOMER)
		SECTOR : mml:Integer
			@i18n:Label (en=Sector)
			@i18n:Tooltip (en="Indicates the sector")
			@t24:i (align=R, inputBehaviour=N, maxLength=4, core=false, t24Name=SECTOR, genOperation="CUSTOMER.NO>CUSTOMER>SECTOR")
		INDUSTRY : mml:Integer
			@i18n:Label (en=Industry)
			@i18n:Tooltip (en="Indicates the Industry")
			@t24:i (align=R, inputBehaviour=N, maxLength=4, core=false, t24Name=INDUSTRY, genOperation="CUSTOMER.NO>CUSTOMER>INDUSTRY")
		TARGET : mml:Integer
			@i18n:Label (en=Target)
			@t24:i (align=R, inputBehaviour=N, maxLength=4, core=false, t24Name=TARGET, genOperation="CUSTOMER.NO>CUSTOMER>TARGET")
		CATEGORY : mml:Integer
			@i18n:Label (en=Category)
			@i18n:Tooltip (en="Specifies the Category")
			@t24:i (align=L, inputBehaviour=N, maxLength=6, core=false, t24Name=CATEGORY, genOperation=PRODUCT.CATEGORY)
		CONV_APPLICATION : mml:Integer
			@t24:i (align=L, inputBehaviour=N, maxLength=30, core=false, t24Name=CONV.APPLICATION, genOperation="SUBR(\"ENQ.TRANS\", \"EU.CONTRACT.CONVERSION\", @ID, \"APPLICATION\")")
		CONVERTED_DATE : T24BusinessTypes:D
			@i18n:Label (en="Converted Date")
			@t24:i (align=R, inputBehaviour=N, maxLength=11, core=false, t24Name=CONVERTED.DATE, genOperation="SUBR(\"ENQ.TRANS\", \"EU.CONTRACT.CONVERSION\", @ID, \"ACTUAL.CONV.DATE\")")
		BALANCE_ID : mml:Integer
			@i18n:Label (en="Swap Balance Id")
			@i18n:Tooltip (en="Swap Balance Id")
			@t24:i (align=L, inputBehaviour=N, maxLength=15, core=false, t24Name=BALANCE.ID, genOperation="@ID : \".A\"")
		BAL_NOTIONAL : mml:Integer
			@i18n:Label (en="Balance Notional")
			@t24:i (align=L, inputBehaviour=N, maxLength=2, core=false, t24Name=BAL.NOTIONAL, genOperation="BALANCE.ID>SWAP.BALANCES>NOTIONAL")
	}
	SWAP__CONDITIONS
	{
		/* Multi-value field used to define any additional agreement type conditions.
Combined with the Swap Agreement Type definition, corresponds to field 77 on Swift messages.
Validation Rules
1-35 Type S Alpha-numeric characters. */
		CONDITIONS : T24BusinessTypes:S
			@t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=18.0, core=true, t24Name=CONDITIONS)
	}
	SWAP__BANK_INFO
	{
		/* Provides instructions or additional information from the sending bank for the receiver, intermediary account with institution or beneficiary institution.
Any details input by the user will be relayed to the receiver.
Whenever this field is used in a message transmitted between countries with different languages, it is recommended that English is used.
This field corresponds to field 72 on Swift messages, and must never be used for information for which another field is intended, of for instructions not required within a message.
(Bank to bank information - Swift field 72).
Validation Rules
Up to 35 Type "S" characters (Uppercase alpha or numeric). Multivalued field.  A maximum of 6 lines of 35 characters each will be allowed.
If this field is used, the first multivalue must contain a code word followed by further information.
All code words must be present on the SWIFT.CODE.WORDS file, and must be enclosed between slashes ("/").
Further code words and information can be entered in subsequent multivalues, but additional information continued on a new line must be preceded by double slashes ("//"). */
		BANK_INFO : T24BusinessTypes:SW
			@t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=20.0, core=true, t24Name=BANK.INFO)
	}
	SWAP__AS_BUS_CENTRES
	{
		/* Indicates those countries whose calendars must be checked with regard to holiday (working day) validation.
The default value will be the Country of the currency of the Asset side of the contract, but additional Country codes can be manually input if required.  For example, the country codes of the participants and/or the Liability leg.
Validation Rules
2 Alpha characters. (Optional input.  Multi-valued field).
The code must be a a valid definition on the COUNTRY table and must have a valid entry on the REGION/HOLIDAY table.
Defaulted to the Country of the Asset leg Currency. */
		AS_BUS_CENTRES : T24BusinessTypes:A
			@t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=40.0, core=true, t24Name=AS.BUS.CENTRES)
	}
	SWAP__AS_TYPE
	{
		/* Defines the type of the Asset Swap schedule.
Can be any valid type as defined on the SWAP.SCHEDULE.TYPE file. The Swap Schedule Type file defines all of the schedules which are permitted on a Swap contract. Details such as the transaction codes to be used on entries, P&L category codes, Charge codes etc., are also defined on the Swap Schedule Type table.
The first two characters of the schedule type ID determine the type of schedule.
When a schedule is processed the Swap balances file (SWAP.BALANCES) is updated accordingly.
Validation Rules

1-6 Alpha-numeric characters.
Must be a valid definition of the SWAP.SCHEDULE.TYPE file. Different schedules can be     added but the first two characters must be one of the specified fixed schedule types.
The schedules AC, AM, AY, CI, and CM, which are system generated schedules, cannot be     input by the user.
Only one "PX"/"IS" schedule and one principal re-exchange     "RX" schedule can be specified on the Asset leg of the contract. Input of an     "RX" scheduled is disallowed if there is no "PX"/"IS"     schedule defined (except on flexible currency swaps)
On deals with the
SWAP.TYPE
FLEX.PRIN.PAYMENT */
		AS_TYPE -> SW_Schedules:SWAP_SCHEDULE_TYPE
			@t24:i (align=L, inputBehaviour=I, maxLength=6, sysNumber=41.0, core=true, businessType=A, t24Name=AS.TYPE)
		/* Specifies the date and frequency of the schedule.
The date of the schedule is defined along with the frequency of future process dates of the schedule. Schedules can be processed on any day of the month and at a frequency of either daily, monthly, quarterly, half-yearly, or yearly.
When a schedule is processed :
o If the schedule has no frequency then the schedule is removed from the swap contract.
o If the schedule has a frequency then the next process date is updated according to the frequency. Eg. Schedule processed on 01 Jan 95 frequency M0101, then the date.freq field is updated with 01 Feb 95 frequency M0101.
The swap balance file, "SWAP.BALANCES", is updated accordingly.
Validation Rules

16 Character standard date/frequency code format (type A         FREQ). (Mandatory input if the associated schedule type         has been defined).
A frequency is disallowed for "PX" and         "RX" schedules.
For all schedules, the date must be GE the value date of         the contract and LE the maturity date of the contract.
The date must be equal to the Asset interest effective         date for schedule type "PX".
The date must be greater than the asset interest         effective date and today for schedule type "IP"         (Interest payment).
If schedule type is IP or AP and Day convention is         Following or Modified then daily and business day freq is         not allowed. */
		AS_DATE_FREQ : T24BusinessTypes:FQO
			@t24:i (align=L, inputBehaviour=I, maxLength=16, sysNumber=42.0, core=true, t24Name=AS.DATE.FREQ)
		AS_INT_SET_DATE : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=16, sysNumber=43.0, core=true, t24Name=AS.INT.SET.DATE)
		/* Defines the final date for the schedule.
If a schedule is defined with a process date and frequency (AS.DATE.FREQ) then the final process date is the date defined in this field.
Validation Rules
16 Alpha-numeric standard date format. (Optional input.  No default). */
		AS_FINAL_SCHED : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=16, sysNumber=44.0, core=true, t24Name=AS.FINAL.SCHED)
		/* The use of this field is dependent on the schedule type. It specifies the amount for which the schedule is to use, ie. schedule amount.
This field is mandatory for schedules the following schedules :
o  PI  Principal increase   o  PD  Principal decrease   o  PM  Payment   o  RV  Receipt   o  IP  Interest payment  (If interest payments are fixed,                             ie. fixed rate (AS.FIXED.RATE) and                             the rate key (AS.RATE.KEY) are null).
Must be null for the following schedules :
o  PX  Principal exchange (uses the outstanding principal amount)   o  RX  Principal re-exchange (uses the outstanding principal          amount).   o  RR  Rate reset (rate specified in rate field AS.RATE)   o  CC  Charge or commission   o  DY  Diary note   o  IP  Interest Payment  (If interest payments are floating. The                             interest amount is calculated using the                             outstanding contract principal amount                             and the current interest rate for the                             period (AS.CURRENT.RATE).
Validation Rules
1-19 Type AMT numeric characters. */
		AS_AMOUNT : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=45.0, core=true, t24Name=AS.AMOUNT, typeModifiers="-22")
		/* Specifies the basis rate to be used on a Rate Reset ("RR") schedule.
The rate reset schedule will not be processed if this field is null.
On processing the rate reset schedule the interest rate (AS.CURRENT.RATE) used for the current interest period will have the spread applied.
Validation Rules
1-11 Numeric characters plus a decimal point. (Optional input).
Only allowed for a Rate Reset ("RR") swap schedule. */
		AS_RATE : mml:Integer
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=46.0, core=true, t24Name=AS.RATE)
		/* Specifies the treasury rate to be used on a Rate Reset ("RR") schedule.
The rate reset schedule for non-treasury customer  will not be processed if this field is null.

On processing the rate reset schedule the treasury rate (AS.CUR.TRSRY.RATE) used for the current interest period will have the spread applied.

Validation Rules

1-11 Numeric characters plus a decimal point. (Optional input).
Only allowed for a Rate Reset ("RR") swap schedule.
Fields AS.TYPE to AS.PROCESSED form a related multi value set. */
		AS_TRSRY_RATE : mml:Integer
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=47.0, core=true, t24Name=AS.TRSRY.RATE)
		/* Defines the end date on the Asset leg to amortise the premium.
Validation Rules
11 characters Standard date format. (Optional input). */
		AS_AMORT_DATE : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=48.0, core=true, t24Name=AS.AMORT.DATE)
		AS_NARR -> byValue SW_Contract:SWAP__AS_TYPE__AS_NARR *
		/* Signifies whether contract details have been sent to the Delivery system for onward transmission to the associated counterparty.
Valid definitions on this field are :
o  null  signifies that no advice has been sent.
o  "Y"   signifies that an advice has been sent.
The field will be updated with Y, even if there is no message to be produced. The updating is to indicate that the Asset Leg has been considered for message processing as per the definitions in the system for the counter party under reference.
Validation Rules
1 Alpha-numeric character. (No-input field.  Default = null). */
		AS_ADVICE_SENT : SW_Contract:SWAP__AS_TYPE__AS_ADVICE_SENT
			@t24:i (align=L, inputBehaviour=N, maxLength=1, sysNumber=50.0, core=true, t24Name=AS.ADVICE.SENT)
		/* Signifies the date that the associated schedule was last processed.
Validation Rules
Standard date format. (No-input field.  Automatically updated by the Swap system). */
		AS_PROCESSED : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=N, maxLength=11, sysNumber=51.0, core=true, t24Name=AS.PROCESSED)
	}
	SWAP__AS_TYPE__AS_NARR
	{
		/* Optional multi-valued field to input additional data that is to be included on advices and confirmations.
Validation Rules
Free format text of up to 4 lines, maximum 35 alpha-numeric characters per line. (Optional input.  Multi-valued field). */
		AS_NARR : T24BusinessTypes:S
			@t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=49.0, core=true, t24Name=AS.NARR)
	}
	SWAP__LB_BUS_CENTRES
	{
		/* Indicates those countries whose calendars must be checked with regard to holiday (working day) validation.
The default value will be the Country of the currency of the Liability side of the contract, but additional Country codes can be manually input if required.  For example, the country codes of the participants and/or the Asset leg.
Validation Rules
2 Alpha characters. (Optional input.  Multi-valued field).
The code must be a a valid definition on the COUNTRY table and must have a valid entry on the REGION/HOLIDAY table.
Defaulted to the Country of the Liability leg Currency. */
		LB_BUS_CENTRES : T24BusinessTypes:A
			@t24:i (align=L, inputBehaviour=I, maxLength=4, sysNumber=70.0, core=true, t24Name=LB.BUS.CENTRES)
	}
	SWAP__LB_TYPE
	{
		/* Defines the type of the Liability Swap schedule.
Can be any valid type as defined on the SWAP.SCHEDULE.TYPE file. The Swap Schedule Type file defines all of the schedules which are permitted on a Swap contract. Details such as the transaction codes to be used on entries, P&L category codes, Charge codes etc., are also defined on the Swap Schedule Type table.
The first two characters of the schedule type id determine the type of schedule.
When a schedule is processed the Swap balances file (SWAP.BALANCES) is updated accordingly.
Validation Rules

1-6 Alpha-numeric characters.
Must be a valid definition of the SWAP.SCHEDULE.TYPE file. Different schedules can be     added but the first two characters must be one of the specified fixed schedule types.
The schedules AC, AM, AY, CI, and CM, which are system generated schedules, cannot be     input by the user.
Only one "PX"/"IS" schedule and one principal re-exchange     "RX" schedule can be specified on the Liability leg of the contract. Input of an     "RX" scheduled is disallowed if there is no "PX"/"IS"     schedule defined(except on flexible currency swaps)
On deals with the
SWAP.TYPE
FLEX.PRIN.PAYMENT */
		LB_TYPE -> SW_Schedules:SWAP_SCHEDULE_TYPE
			@t24:i (align=L, inputBehaviour=I, maxLength=6, sysNumber=71.0, core=true, businessType=A, t24Name=LB.TYPE)
		/* Specifies the date and frequency of the schedule.
The date of the schedule is defined along with the frequency of future process dates of the schedule. Schedules can be processed on any day of the month and at a frequency of either daily, monthly, quarterly, half-yearly, or yearly.
When a schedule is processed :
o If the schedule has no frequency then the schedule is removed from the swap contract.
o If the schedule has a frequency then the next process date is updated according to the frequency. Eg. Schedule processed on 01 Jan 95 frequency M0101, then the date.freq field is updated with 01 Feb 95 frequency M0101.
The swap balance file, "SWAP.BALANCES", is updated accordingly.
Validation Rules

16 Character standard date/frequency code format (type A         FREQ). (Mandatory input if the associated schedule type         has been defined).
A frequency is disallowed for "PX" and         "RX" schedules.
For all schedules, the date must be GE the value date of         the contract and LE the maturity date of the contract.
The date must be equal to the Liability interest         effective date for schedule type "PX".
The date must be greater than the liability interest         effective date and today for schedule type "IP"         (Interest payment).
If schedule type is IP or AP and Day convention is         Following or Modified then daily and business day freq is         not allowed. */
		LB_DATE_FREQ : T24BusinessTypes:FQO
			@t24:i (align=L, inputBehaviour=I, maxLength=16, sysNumber=72.0, core=true, t24Name=LB.DATE.FREQ)
		LB_INT_SET_DATE : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=16, sysNumber=73.0, core=true, t24Name=LB.INT.SET.DATE)
		/* Defines the final date for the schedule.
If a schedule is defined with a process date and frequency (LB.DATE.FREQ) then the final process date is the date defined in this field.
Validation Rules
16 Alpha-numeric standard date format. (Optional input.  No default). */
		LB_FINAL_SCHED : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=16, sysNumber=74.0, core=true, t24Name=LB.FINAL.SCHED)
		/* The use of this field is dependent on the schedule type. It specifies the amount for which the schedule is to use, ie. schedule amount.
This field is mandatory for schedules the following schedules :
o  PI  Principal increase   o  PD  Principal decrease   o  PM  Payment   o  RV  Receipt   o  IP  Interest payment  (If interest payments are fixed,                             ie. fixed rate (LB.FIXED.RATE) and                             the rate key (LB.RATE.KEY) are null).
Must be null for the following schedules :
o  PX  Principal exchange (uses the outstanding principal amount)   o  RX  Principal re-exchange (uses the outstanding principal          amount).   o  RR  Rate reset (rate specified in rate field LB.RATE)   o  CC  Charge or commission   o  DY  Diary note   o  IP  Interest Payment  (If interest payments are floating. The                             interest amount is calculated using the                             outstanding contract principal amount                             and the current interest rate for the                             period (LB.CURRENT.RATE).
Validation Rules
1-19 Type AMT numeric characters. */
		LB_AMOUNT : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=75.0, core=true, t24Name=LB.AMOUNT, typeModifiers="-52")
		/* Specifies the basis rate to be used on a Rate Reset ("RR") schedule.
The rate reset schedule will not be processed if this field is null.
On processing the rate reset schedule the interest rate (LB.CURRENT.RATE) used for the current interest period will have the spread applied.
Validation Rules
1-11 Numeric characters plus a decimal point. (Optional input).
Only allowed for a Rate Reset ("RR") swap schedule. */
		LB_RATE : mml:Integer
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=76.0, core=true, t24Name=LB.RATE)
		/* Specifies the treasury rate to be used on a Rate Reset ("RR") schedule for the liability leg of the contract.

The rate reset schedule for non-treasury customer  will not be processed if this field is null.

On processing the rate reset schedule the treasury rate (LB.CUR.TRSRY.RATE) used for the current interest period will have the spread applied.

Validation Rules

1-11 Numeric characters plus a decimal point. (Optional input).
Only allowed for a Rate Reset ("RR") swap schedule.
Fields LB.TYPE to LB.PROCESSED form a related multi value set. */
		LB_TRSRY_RATE : mml:Integer
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=77.0, core=true, t24Name=LB.TRSRY.RATE)
		/* Defines the end date on the Liability leg to amortise the premium.
Validation Rules
11 characters Standard date format. (Optional input). */
		LB_AMORT_DATE : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=78.0, core=true, t24Name=LB.AMORT.DATE)
		LB_NARR -> byValue SW_Contract:SWAP__LB_TYPE__LB_NARR *
		/* Signifies whether contract details have been sent to the Delivery system for onward transmission to the associated counterparty.
Valid definitions on this field are :
o  null  signifies that no advice has been sent.
o  "Y"   signifies that an advice has been sent.
The field will be updated with Y, even if there is no message to be produced. The updating is to indicate that the Liability Leg has been considered for message processing as per the definitions in the system for the counter party under reference.
Validation Rules
1 Alpha-numeric character. (No-input field.   Default = null). */
		LB_ADVICE_SENT : SW_Contract:SWAP__LB_TYPE__LB_ADVICE_SENT
			@t24:i (align=L, inputBehaviour=N, maxLength=1, sysNumber=80.0, core=true, t24Name=LB.ADVICE.SENT)
		/* Signifies the date that the associated schedule was last processed.
Validation Rules
Standard date format. (No-input field.   Automatically updated by the Swap system). */
		LB_PROCESSED : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=N, maxLength=11, sysNumber=81.0, core=true, t24Name=LB.PROCESSED)
	}
	SWAP__LB_TYPE__LB_NARR
	{
		/* Optional multi-valued field to input additional data that is to be included on advices and confirmations.
Validation Rules
Free format text of up to 4 lines, maximum 35 alpha-numeric characters per line. (Optional input.   Multi-valued field). */
		LB_NARR : T24BusinessTypes:S
			@t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=79.0, core=true, t24Name=LB.NARR)
	}
	SWAP__SET_CURRENCY
	{
		/* Identifies the currency for contract settlements.
This field is part of a set of settlement instructions to which credits and debits of a swap contract are made.
There must be a set of settlement instructions for each currency defined on the contract.
Therefore if the asset leg currency is the same as the liability leg currency, then one set of settlement instructions with a settlement currency of the same must be defined. The settlement currency is defaulted to this currency.
If the leg currencies are different then two sets of settlement instructions are required (one for each currency).
Both settlement currencies are defaulted - one to each leg of the contract.
Validation Rules
1-3 Characters Type CCY (Currency code). (Mandatory input.  Defaults to leg currencies).
Must be a valid currency code as defined on the Currency file. */
		SET_CURRENCY -> FIN_CurrencyConfig:CURRENCY
			@t24:i (align=L, inputBehaviour=I, maxLength=3, sysNumber=82.0, core=true, businessType=CCY, t24Name=SET.CURRENCY)
		/* Specifies the type of entries that are to be posted to the account number specified in the corresponding set of settlement instructions.
Valid definitions are as follows :
o  null     All accounting entries are to be posted to the               corresponding settlement account number.
o  PAYMENT  Only "payment" accounting entries are to be posted               to the corresponding settlement account number.
o  RECEIPT  Only "receipt" accounting entries are to be posted               to the corresponding settlement account number.
The set up of this field should be either :
o  null   or,   o  a set of defined instructions for both "PAYMENT" and      "RECEIPT".
The main key to the settlement instructions to identify the associated account number is the Currency (SET.CURRENCY).
If a set of settlement instructions for a currency is defined with a type of "PAYMENT", and a set of associated instructions for "RECEIPT" is not defined, then the same settlement account will be used for both payment and receipt type entries.
Validation Rules
1-8 Alpha-numeric characters. (Optional input.  Default = null).
Valid definitions are null, "PAYMENT", and "RECEIPT". */
		SET_PAY_RECEIPT : SW_Contract:SWAP__SET_CURRENCY__SET_PAY_RECEIPT
			@t24:i (align=L, inputBehaviour=I, maxLength=8, sysNumber=83.0, core=true, t24Name=SET.PAY.RECEIPT)
		/* Specifies the type of movements that are to be posted to the account specified in the corresponding set of settlement instructions.
Valid definitions are as follows :
o  null       All movements are to be posted to the                 corresponding settlement account number.   o  PRINCIPAL  Only "prinicpal" movements are to be posted to                 the corresponding settlement account number.   o  INTEREST   Only "interest" movements are to be posted to                 the corresponding settlement account number.
Either :
o  one set of instructions should be defined for the currency      with type set to null
o  or, two sets of instructions should be defined for the      currency - one each type, "PRINCIPAL and "INTEREST".
The main key to the settlement instructions to identify the associated currency account number is the currency (SET.CURRENCY).
Validation Rules
1-12  Alpha-numeric characters. (Optional input. Default = null).
Valid definitions are null, "PRINCIPAL", and "INTEREST". */
		SET_TYPE : SW_Contract:SWAP__SET_CURRENCY__SET_TYPE
			@t24:i (align=L, inputBehaviour=I, maxLength=12, sysNumber=84.0, core=true, t24Name=SET.TYPE)
		/* Identifies the Account number for the specified set of settlement instructions.
It defines the account to be credited or debited on the contract value date and for the appropriate contract schedules.
Should the account indicated be a Nostro account then further input may be required in the appropriate fields listed below :
o  ACCT.WITH.BANK   o  ACC.WITH.ADD   o  BEN.ACCOUNT
The system will default to the first current account of the customer for the currency (SET.CURRENCY).  The account indicated in this field will be used to default the remaining customer/ account fields within the settlement instructions if no further details are specified.
Validation Rules
1-16 numeric characters relating to a Customer account or a Nostro Account. Or, 3-10 type A (alpha-numeric) account mnemonic. (Mandatory input.  Default = Current account in settlement ccy).
This field will only be defaulted if the settlement currency field (SET.CURRENCY) is left blank.
Must be of the same currency as defined in the settlement currency (SET.CURRENCY).
This field can be changed at any time, but after the Value date of the contract, it will only affect future contract schedule movements. */
		ACCOUNT_NUMBER -> ACM_AccountOpening:ACCOUNT
			@t24:i (align=L, inputBehaviour=I, maxLength=16, sysNumber=85.0, core=true, businessType=ANT, t24Name=ACCOUNT.NUMBER)
		/* Identifies the Customer code of any intermediary bank involved in the contract.  This will be specified by the counterparty.
If the intermediary is known (ie. there is an associated customer record), the customer number of the intermediary bank will be entered.  If not, details should be entered into the INTERM.ADD field.
This field will be defaulted only if the settlement currency field (SET.CURRENCY) is left blank.  The intermediary will be defaulted from CUSTOMER.CCY.ACCT table and then, if not identified, from the NOSTRO.ACCOUNT table and the AGENCY table.
Validation Rules
1 to 6 Numeric character Customer code, or, 3 to 10 character Customer mnemonic. (Optional input.  Default = See Details.)
No input allowed if INTERM.ADD is defined. */
		INTERMEDIARY -> ST_Customer:CUSTOMER
			@t24:i (align=R, inputBehaviour=I, maxLength=10, sysNumber=86.0, core=true, businessType=CUS, t24Name=INTERMEDIARY)
		INTERM_ADD -> byValue SW_Contract:SWAP__SET_CURRENCY__INTERM_ADD *
		/* Specifies the Customer code of the correspondent bank.
This field will be defaulted only if the settlement currency field (SET.CURRENCY) is left blank.  It will be defaulted from the CUSTOMER.CCY.ACCT table and then, if not identified, from the NOSTRO.ACCOUNT table and the AGENCY table.
Validation Rules
1 to 6 Numeric character Customer code, or 3 to 10 Customer mnemonic. (Optional input.  Default = See Details).
For a Nostro account, must have this field defined or ACC.WITH.ADD, but not both. */
		ACCT_WITH_BANK -> ST_Customer:CUSTOMER
			@t24:i (align=R, inputBehaviour=I, maxLength=10, sysNumber=88.0, core=true, businessType=CUS, t24Name=ACCT.WITH.BANK)
		ACC_WITH_ADD -> byValue SW_Contract:SWAP__SET_CURRENCY__ACC_WITH_ADD *
		/* Defines the contract beneficiary account number.
This field is used when the counterparty of the contract instructs payment/receipt to be made to another party who does not have an account the associated bank.
Validation Rules
1 to 35 Type S Alphanumeric characters. (Mandatory when ACC.WITH.BANK or ACC.WITH.ADD has been  specified.  Default = ).
May contain whatever account details required, in order to uniquely identify the Beneficiary account. */
		BEN_ACCOUNT : T24BusinessTypes:S
			@t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=90.0, core=true, t24Name=BEN.ACCOUNT)
		BANK_NARR -> byValue SW_Contract:SWAP__SET_CURRENCY__BANK_NARR *
	}
	SWAP__SET_CURRENCY__INTERM_ADD
	{
		/* Defines the address of the intermediary address.
If an intermediary bank is used and the customer number of the intermediary is not known (INTERMEDIARY), this multi-valued field should contain the name and address of the intermediary.
Validation Rules
3 to 35 alphanumeric Swift characters. (Optional input).
No input allowed if INTERMEDIARY has been defined.
A maximun of four lines is allowed. */
		INTERM_ADD : T24BusinessTypes:S
			@t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=87.0, core=true, t24Name=INTERM.ADD)
	}
	SWAP__SET_CURRENCY__ACC_WITH_ADD
	{
		/* Defines the Bank's address.
If the Customer account of the back is unknown, ACCT.WITH.BANK is undefined, then this multi-valued field should contain the name and address of the bank.
Validation Rules
3 to 35 alphanumeric Swift characters. (Optional input).
No input allowed should ACCT.WITH.BANK be defined.
A maximum of four lines is allowed. */
		ACC_WITH_ADD : T24BusinessTypes:S
			@t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=89.0, core=true, t24Name=ACC.WITH.ADD)
	}
	SWAP__SET_CURRENCY__BANK_NARR
	{
		/* Provides instructions or additional information from the Sending bank for the receiver, intermediary account with institution, or beneficiary institution.
For outward payments, any details input will be relayed to the Receiver bank.
For Inward payments/receipts which are processed directly from Delivery, all transactions where bank to bank information has been found will be placed in hold status (IHLD) to force the user to review the bank to bank information from the sending bank.
Whenever this field is used in a message transmitted between countries with different languages, it is recommended that English is used.
This field corresponds to field 72 on SWIFT messages, and must never be used for information for which another field is intended, or for instructions not required in a message.
Validation Rules
Up to 35 Type S characters (uppercase alpha or numeric). Multi-valued field. A maximum of 6 lines of 35 characters each will be allowed.
If this field is used, the first multivalue must contain a code word followed by further information.
All Swift code words must be present on the SWIFT.CODE.WORDS file, and must be enclosed between slashes "/".
Further code words and information can be entered in subsequent multivalues, but additional information contained on a new line must be preceded by double slashes "//". */
		BANK_NARR : T24BusinessTypes:SW
			@t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=91.0, core=true, t24Name=BANK.NARR)
	}
	SWAP__ACTIVITY
	{
		/* This field displays the Swap activity code, as defined on the SWAP.ACTIVITY file, for which the delivery message was raised.
This field is a no-input field and is part of a multi-valued set.
Example Swap Activity codes :
o  101  Contract confirmation   o  102  Principal exchange   o  103  Principal increase/decrease   o  110  Payment of premium   o  202  Payment
Validation Rules
3 Numeric characters. (No-input field.  Default = Automatically updated by the system).
Any valid definition on the SWAP.ACTIVITY file. */
		ACTIVITY -> SW_Delivery:SWAP_ACTIVITY
			@t24:i (align=R, inputBehaviour=N, maxLength=3, sysNumber=107.0, core=true, t24Name=ACTIVITY)
		/* Identifies the net payment amount of a 202 (Payment) or 210 (Advice to receive) swap activity.
Part of a multi-valued set of Delivery details.
Validation Rules
1-19 Type AMT numeric characters (Amount field). (No-input field).
Automatically updated with the net amount relating to a 202 or 210 activity. */
		NET_AMOUNT : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=N, maxLength=19, sysNumber=108.0, core=true, t24Name=NET.AMOUNT, typeModifiers="22")
		NET_PAY_VAL_DT : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=N, maxLength=11, sysNumber=109.0, core=true, t24Name=NET.PAY.VAL.DT)
		/* Identifies the leg type associated with the set of Delivery details.
Will be defined as A (asset) or L (liability).
Validation Rules
1 Alpha character. (No-input.  Default = Swap leg type "A" or "L"). */
		ACT_LEG_TYPE : T24BusinessTypes:A
			@t24:i (align=L, inputBehaviour=N, maxLength=1, sysNumber=110.0, core=true, t24Name=ACT.LEG.TYPE)
		/* Internal index that references back to the swap schedule associated with the set of Delivery details.
Validation Rules
1-2 Numeric characters. (No-input field.  Default = index back to associated swap schedule). */
		ACT_SCHED_INDEX : mml:Integer
			@t24:i (align=R, inputBehaviour=N, maxLength=2, sysNumber=111.0, core=true, t24Name=ACT.SCHED.INDEX)
		MESSAGE -> byValue SW_Contract:SWAP__ACTIVITY__MESSAGE *
	}
	SWAP__ACTIVITY__MESSAGE
	{
		/* Confirmation that the contract details have been passed over to the Delivery system for onward communication to the counterparty.
This field is defaulted to the Delivery message Id (DE.MESSAGE) of the message sent to the Delivery system.
Eg. 360 = Single currency IRS confirmation     361 = Multi currency IRS confirmation     362 = Interest Rate Swap Reset.
Validation Rules
3 Numeric character. (No-input field.  Default = DE.MESSAGE Id). */
		MESSAGE -> DE_Config:DE_MESSAGE
			@t24:i (align=R, inputBehaviour=N, maxLength=3, sysNumber=112.0, core=true, t24Name=MESSAGE)
		/* Identifies the date that the associated Delivery message was produced or "NOT PRODUCED" for messages not yet made.
Validation Rules
12 Alphanumeric characters. (No-input field.  Default = See details.)
Must be a valid date or "NOT PRODUCED". */
		MSG_DATE : T24BusinessTypes:A
			@t24:i (align=L, inputBehaviour=N, maxLength=12, sysNumber=113.0, core=true, t24Name=MSG.DATE)
		MSG_REF : T24BusinessTypes:A
			@t24:i (align=L, inputBehaviour=N, maxLength=16, sysNumber=114.0, core=true, t24Name=MSG.REF)
	}
	SWAP__CONTACT_DETAILS
	{
		/* This field specifies the contact name and telephone number the Receiver may contact for any queries concerning this transaction.
This field corresponds to SWIFT field 29A.
Validation Rules
Up to 35 type S characters.
Maximum of 4 multi values is permitted. */
		CONTACT_DETAILS : T24BusinessTypes:SW
			@t24:i (align=L, inputBehaviour=I, maxLength=24, sysNumber=118.0, core=true, t24Name=CONTACT.DETAILS)
	}
	SWAP__AS_INTEREST_DET
	{
		/* The interest details of the asset leg of the swap transaction are stored here. This field will only be used when the assest leg is floating, and is used when the MARKET.RATE.TEXT record does not hold sufficient detail for the generation of the SWIFT message. In this case the value 'OTHER' is used in field 14F on the SWIFT message and the value in this field is used.
This field will not be used if the asset leg is fixed.
Validation Rules
Mandatory Input when the MARKET.RATE.TEXT record specified in the field AS.RATE.KEY has a rate text which does not correspond to the format       alpha / alpha / alpha.  Refer to the helptext on field RATE.TEXT on the file MARKET.RATE.TEXT
A maximum of six multi values are allowed for this field. */
		AS_INTEREST_DET : T24BusinessTypes:S
			@t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=119.0, core=true, t24Name=AS.INTEREST.DET)
	}
	SWAP__LB_INTEREST_DET
	{
		/* The interest details of the liability leg of the swap transaction are stored here. This field will only be used when the liability leg is floating, and is used when the MARKET.RATE.TEXT record does not hold sufficient detail for the generation of the SWIFT message. In this case the value 'OTHER' is used in field 14F on the SWIFT message and the value in this field is used.
This field will not be used if the liability leg is fixed.
Validation Rules
Mandatory Input when the MARKET.RATE.TEXT record specified in the field LB.RATE.KEY has a rate text which does not correspond to the format       alpha / alpha / alpha.  Refer to the helptext on field RATE.TEXT on the file MARKET.RATE.TEXT
A maximum of six multi values are allowed for this field. */
		LB_INTEREST_DET : T24BusinessTypes:S
			@t24:i (align=L, inputBehaviour=I, maxLength=35, sysNumber=120.0, core=true, t24Name=LB.INTEREST.DET)
	}
	SWAP__ENTRY_IDS
	{
		/* Multi-valued field which identifies the entries raised by the contract.
The entry Ids are generated automatically by the system and identify either a statement entry or a category entry in respect of the contract.
The number automatically generated is unique to the associated contract and is built in the following way :
Eg.  673732684.48
o  The first four digits "6737" indicates the number of days      since the 1st of January 1968.   o  The next five "32684" indicates the number of seconds since      midnight.   o  The last two digits "48" indicates the number of hundreds      of seconds.
Validation Rules
29 Alphanumeric characters. (No-input.  System generated). */
		ENTRY_IDS : T24BusinessTypes:A
			@t24:i (align=L, inputBehaviour=N, maxLength=29, sysNumber=135.0, core=true, t24Name=ENTRY.IDS)
	}
	SWAP__OVERRIDE
	{
		/* Contains all the Override messages which the user agreed to during input of the contract.
During the validation stage of a swap contract, the system may provide the user with a series of screen messages to indicate warnings - such as "Date Entered Is A Non-working Day". In the event that the user confirms the override, the full text of the override message will be stored, for reference purposes only, in this multi-valued field.
Validation Rules
1-35 Alphanumeric characters. (No-input field.  Default = See details). */
		OVERRIDE : T24BusinessTypes:A
			@t24:i (align=L, inputBehaviour=N, maxLength=35, sysNumber=136.0, core=true, t24Name=OVERRIDE)
	}
	SWAP__INPUTTER
	{
		INPUTTER : T24BusinessTypes:A
			@t24:i (align=L, inputBehaviour=N, maxLength=40, sysNumber=139.0, core=true, t24Name=INPUTTER)
	}
	SWAP__DATE_TIME
	{
		DATE_TIME : T24BusinessTypes:RELTIME
			@t24:i (align=L, inputBehaviour=N, maxLength=15, sysNumber=140.0, core=true, t24Name=DATE.TIME, mask="RDD DD  DD ##:##")
	}
	/* The SWAP.BALANCES file records all financial movements and balance details of a particular Swap contract (including all balance information, principal outstanding, accruals, interest paid/received, rate changes, etc.).
Both current and historical details are maintained providing a means by which the system can control and record all financial transactions.
This file also provides a valuable source of information for enquiry and reporting purposes for both bank and customer requirements.
The file is keyed by contract number and leg identifier (asset or liability).
Two SWAP.BALANCES records, Asset and Liability, are created whenever a Swap contract is entered into the system and remain there as long as the contract remains "Live". Eg. SW9503000001.A and SW9503000001.L
The file is updated automatically by any Swap routines which modify any balance details.  There is no facility for direct input to this file.  The only functions that users are allowed to perform are "L"ist, "S"ee, and "P"rint.
A corresponding save file and history file exist, namely SWAP.BALANCES.SAVE and SWAP.BALANCES.HIST, which will be a copy of the data held in the SWAP.BALANCES file. The save file stores the last authorised version of the balances record whereas the history file stores the reversed version.
Whenever a Swap schedule is processed on the Swap contract, it is removed from the contract and it's details held on the relevant SWAP.BALANCES record.
The following associated information is held on this file :
o  Currency of the leg.   o  Indication as to whether the principal is NOTIONAL or not.   o  Outstanding principal details.   o  Effective and historical interest rates.   o  Interest amounts and accrual details.   o  Amortisation details.   o  Schedules processed details.   o  Corresponding CRB details. */
	SWAP_BALANCES
		@t24:i (t24Name=SWAP.BALANCES, level=F, type=L, allowedFunctions="L P S", localRefAllowed=false)
	{
		/* This field uniquely identifies each record on the SWAP.BALANCES file.
The Id is keyed by the associated Swap contract number and leg identifier (asset or liability).
Eg.  SW950300001.A or SW950300001.L.
The Id has the following format SWyydddnnnnn.X, where :
o  SW     Application indicator for the Swaps application.   o  yy     Last two digits of the year when the transaction             was processed.   o  ddd    Julian date number within the year when the Swap             transaction was processed.   o  nnnnn  Five digit sequential number.   o  .X     Leg identifier ".A" asset or ".L" liability.
Validation Rules
30 Alpha-numeric characters. (No-input).
The Id is automatically generated when a Swap contract is input (two Swap balances records are created for one Swap contract). */
		CONTRACT_NUMBER -> SW_Contract:SWAP_BALANCES PK required
			@i18n:Label (en="Contract Number")
			@t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=0.0, core=true, businessType=A, t24Name=CONTRACT.NUMBER)
		/* This field denotes the currency code of the associated Swap leg.
Validation Rules
3 Alpha-numeric currency code (type CCY). (No-input field.  Automatically updated).
Must be a valid CURRENCY code. */
		CURRENCY : T24BusinessTypes:CCY
			@i18n:Label (en=Ccy)
			@t24:i (align=L, inputBehaviour=I, maxLength=3, sysNumber=1.0, core=true, t24Name=CURRENCY)
		/* This field indicates whether the outstanding principal is notional or real.
o  "Y"   signifies a Notional principal amount.   o  "NO"  signifies a Real principal amount.
The principal amount will remain notional until a PX (Principal Exchange) schedule has been processed on the Swap leg.
This field is automatically updated.
Validation Rules
2 Alphanumric characters. (No-input field.  Valid types : "Y"_"NO"). */
		NOTIONAL : T24BusinessTypes:A
			@t24:i (align=L, inputBehaviour=I, maxLength=2, sysNumber=2.0, core=true, t24Name=NOTIONAL)
		PRINCIPAL -> byValue SW_Contract:SWAP_BALANCES__PRINCIPAL *
		EFFECTIVE_DATE -> byValue SW_Contract:SWAP_BALANCES__EFFECTIVE_DATE *
		/* This field records the total amount of unpaid interest due (accrued) for the current interest period.
As interest is accrued, the details of each accrual are stored in the multivalue accrual fields (ACCR.FROM.DATE - ACCR.ACT.AMT). This field contains the total of accrued interest in the current period.  This is equal to the sum of the multivalues in the ACCR.AMT field whose corresponding ACCR.FROM.DATE is greater than or equal to the START.INT.PERIOD date.
Validation Rules
1 to 19 Numeric characters (type AMT). (No-input field). */
		INTEREST_AMOUNT : T24BusinessTypes:AMT
			@i18n:Label (en="Interest Amt")
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=9.0, core=true, t24Name=INTEREST.AMOUNT, typeModifiers="1")
		/* It contains the Interest Amount of the Market Exchange for the current period
As interest is accrued, the details of each accrual are stored in the multivalue accrual fields (ACCR.FROM.DATE - MKT.ACT.AMT). This field contains the total of accrued interest in the current period. This is equal to the sum of the multivalues in the MKT.ACCR.AMT field whose corresponding ACCR.FROM.DATE is greater than or equal to the START.INT.PERIOD date.

Validation Rules
1 to 19 Numeric characters (type AMT). */
		MKT_INT_AMOUNT : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=10.0, core=true, t24Name=MKT.INT.AMOUNT, typeModifiers="1")
		/* Identifies the start date of the current interest period.
This field records the date from which interest will be accrued for the current interest period.  The end of this period is defined in theEND.INT.PERIOD field.
This field is cycled automatically by the end of day batch processing whenever an interest payment is made.  The START.INT.PERDIO date will then become equal to the END.INT.PERIOD date of the period processed.
Validation Rules
11 Numeric Character field (standard date format). (No-input field). */
		START_INT_PERIOD : T24BusinessTypes:D
			@i18n:Label (en="Start Int Period")
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=11.0, core=true, t24Name=START.INT.PERIOD)
		/* Identifies the end date of current interest period.
This field defines the date on which the interest accrued in the current interest period is to be settled.  Accruals for the current interest period commence on the date held in the START.INT.PERIOD field.
The date in this field will be cycled automatically by the end of day batch process whenever an interest payment is made.  It is updated with the next interest payment date as defined on the Swap contract.  The old date from this field then becomes the START.INT.PERIOD date for the coming period.
Validation Rules
11 Numeric Character field (standard date format). (No-input field). */
		END_INT_PERIOD : T24BusinessTypes:D
			@i18n:Label (en="End Int Period")
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=12.0, core=true, t24Name=END.INT.PERIOD)
		/* This represents the Total Interest Amount Payable/Receivable on the Asset/Liability Leg of the SWAP Contract from value date till maturity date. This is different from the field Interest.Amount of the SWAP.BALANCES which holds the interest amount for the current period only.

Validation Rules
1 to 19 Numeric characters (type AMT). */
		TOT_INT_AMT : T24BusinessTypes:AMT
			@i18n:Label (en="Total Intt Amt")
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=13.0, core=true, t24Name=TOT.INT.AMT, typeModifiers="1")
		/* This field contains the Total Interest Amount(Net interest) of the Market Exchange.

Validation Rules
1 to 19 Numeric characters (type AMT). */
		TOT_MKT_INT_AMT : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=14.0, core=true, t24Name=TOT.MKT.INT.AMT, typeModifiers="1")
		ACCR_FROM_DATE -> byValue SW_Contract:SWAP_BALANCES__ACCR_FROM_DATE *
		AMORT_SCHEDULE -> byValue SW_Contract:SWAP_BALANCES__AMORT_SCHEDULE *
		SCHEDULE_TYPE -> byValue SW_Contract:SWAP_BALANCES__SCHEDULE_TYPE *
		/* Identifies the CRB recorded value date for the contract.
Validation Rules
11 Character field (standard date format). (No-input field). */
		CRB_VALUE_DATE : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=43.0, core=true, t24Name=CRB.VALUE.DATE)
		/* Identifies the CRB recorded maturity date of the contract.
Validation Rules
11 Character field (standard date format). (No-input field). */
		CRB_MATURITY_DATE : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=44.0, core=true, t24Name=CRB.MATURITY.DATE)
		/* Identifies the CRB recorded next interest date of the contract.
Validation Rules
11 Character field (standard date format). (No-input field). */
		CRB_INTEREST_DATE : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=45.0, core=true, t24Name=CRB.INTEREST.DATE)
		/* Identifies the associated CRB consol key.
This key is the reference to the CONSOLIDATE.ASST.LIAB file.
Validation Rules
1 to 16 Alpha-numeric characters. (No-input field). */
		CONSOL_KEY : T24BusinessTypes:A
			@i18n:Label (en="Consol Key")
			@t24:i (align=L, inputBehaviour=I, maxLength=16, sysNumber=46.0, core=true, t24Name=CONSOL.KEY)
		/* This field is populated when a 'PX' schedule is processed and the contract must be OFF balance sheet. It will hold the exchange rate between the leg currency and the local currency and is used to derive the local currency equivalent of the base currency of the swap contract.
Validation Rules
Standard T24 rate field. */
		INITIAL_XRATE : T24BusinessTypes:R
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=47.0, core=true, t24Name=INITIAL.XRATE)
		/* It holds the principal outstanding in local currency.
All principal movements will update this field.
Validation Rules
1-19 type AMT (standard amount format) characters plus a decimal point. */
		OUTS_PRIN_LCY : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=48.0, core=true, t24Name=OUTS.PRIN.LCY, typeModifiers="USD")
		/* This field holds the net amount which has been posted as unrealised P&L in local currency.
The amount is derived by the formula:- Principal outstanding * (current.xrate - initial.xrate)
Validation Rules
1-19 type AMT (standard amount format) characters plus a decimal point. */
		CCY_REVAL_PL : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=49.0, core=true, t24Name=CCY.REVAL.PL, typeModifiers="USD")
		/* The Net Present Value of this leg.
Validation Rules
1-19 Type AMT numeric characters */
		NPV : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=50.0, core=true, t24Name=NPV, typeModifiers="1")
		/* The Net Present Value local equivalent of this leg.
Validation Rules
1-19 Type AMT numeric characters */
		NPV_LCY : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=51.0, core=true, t24Name=NPV.LCY, typeModifiers="USD")
		POSITION_DATE -> byValue SW_Contract:SWAP_BALANCES__POSITION_DATE *
		CONF_SENT : mml:Integer
			@t24:i (align=R, inputBehaviour=N, maxLength=55, sysNumber=55.0, core=true, t24Name=CONF.SENT)
		/* It holds the principal outstanding in local currency as on the previous day.
Value available in the field OUTS.PRIN.LCY is moved to this field before SWAP Currency Revaluation is done.
Used for reports.
Validation Rules
1-19 type AMT (standard amount format) characters plus a decimal point. */
		OLD_OUTS_PRIN_LCY : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=56.0, core=true, t24Name=OLD.OUTS.PRIN.LCY)
		NPV_BEF_ACC_ADJ : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=57.0, core=true, t24Name=NPV.BEF.ACC.ADJ)
		NPV_BEF_ADJ_LCY : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=58.0, core=true, t24Name=NPV.BEF.ADJ.LCY)
		/* This field contains a value only if SPL.NPV.PROCESS in SWAP.REVAL.PARAMETER is set to Yes

Indicates the Net Present value of this leg , in the same currency, including the Interest Payment amount due on the date of revaluation.

Validation Rules
1-19 Type AMT numeric characters. */
		NPV_INC_TDY_IP : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=59.0, core=true, t24Name=NPV.INC.TDY.IP)
		/* Indicates the Net Present Value of this leg in  local equivalent including the Interest Payment amount due on the date of revaluation.

Validation Rules
1-19 Type AMT numeric characters. */
		NPV_INC_TDY_IPLCY : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=60.0, core=true, t24Name=NPV.INC.TDY.IPLCY)
		CCY_REVAL_FCY : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=61.0, core=true, t24Name=CCY.REVAL.FCY)
		MATURITY_DATE : mml:Integer
			@i18n:Label (en="Maturity Date")
			@i18n:Tooltip (en="Indicates the Maturity Date of the Transaction")
			@t24:i (align=L, inputBehaviour=N, maxLength=11, core=false, t24Name=MATURITY.DATE, genOperation="CONTRACT.ID>SWAP>MATURITY.DATE")
		CONTRACT_STATUS : mml:Integer
			@i18n:Label (en="Contract Status")
			@t24:i (align=L, inputBehaviour=N, maxLength=3, core=false, t24Name=CONTRACT.STATUS, genOperation="CONTRACT.ID>SWAP>CONTRACT.STATUS")
		SOURCE_ID : T24BusinessTypes:A
			@i18n:Label (en="Source Id")
			@t24:i (align=L, inputBehaviour=N, maxLength=30, core=false, t24Name=SOURCE.ID, genOperation="@ID")
		CONTRACT_FILE : mml:Integer
			@t24:i (align=L, inputBehaviour=N, maxLength=4, core=false, t24Name=CONTRACT.FILE, genOperation="\"SWAP\"")
		SECTOR : mml:Integer
			@i18n:Label (en=Sector)
			@i18n:Tooltip (en="Indicates the sector")
			@t24:i (align=R, inputBehaviour=N, maxLength=4, core=false, t24Name=SECTOR, genOperation="CONTRACT.ID>SWAP>SECTOR")
		INDUSTRY : mml:Integer
			@i18n:Label (en=Industry)
			@i18n:Tooltip (en="Indicates the Industry")
			@t24:i (align=R, inputBehaviour=N, maxLength=4, core=false, t24Name=INDUSTRY, genOperation="CONTRACT.ID>SWAP>INDUSTRY")
		ACCOUNT_OFFICER : mml:Integer
			@i18n:Label (en="Account Officer")
			@i18n:Tooltip (en="Indicates Account Officer responsible")
			@t24:i (align=R, inputBehaviour=N, maxLength=4, core=false, t24Name=ACCOUNT.OFFICER, genOperation="CONTRACT.ID>SWAP>ACCOUNT.OFFICER")
		TARGET : mml:Integer
			@i18n:Label (en=Target)
			@t24:i (align=R, inputBehaviour=N, maxLength=4, core=false, t24Name=TARGET, genOperation="CONTRACT.ID>SWAP>TARGET")
		CATGEORY : mml:Integer
			@t24:i (align=R, inputBehaviour=N, maxLength=5, core=false, t24Name=CATGEORY, genOperation="CONTRACT.ID>SWAP>PRODCAT")
		RESIDENCE : mml:Integer
			@i18n:Label (en=Residence)
			@t24:i (align=L, inputBehaviour=N, maxLength=2, core=false, t24Name=RESIDENCE, genOperation="CONTRACT.ID>SWAP>RESIDENCE")
		CONTRACT_ID : mml:Integer
			@i18n:Label (en="Contract Id")
			@t24:i (align=L, inputBehaviour=N, maxLength=30, core=false, t24Name=CONTRACT.ID, genOperation="@ID[\'.\',1,1]")
		ASSET_OR_LIAB : mml:Integer
			@t24:i (align=L, inputBehaviour=N, maxLength=3, core=false, t24Name=ASSET.OR.LIAB, genOperation="@ID[\'.\',2,1]")
		INTEREST_BASIS : mml:Integer
			@i18n:Label (en="Interest Basis")
			@i18n:Tooltip (en="Indicates the interest day basis")
			@t24:i (align=L, inputBehaviour=N, maxLength=3, core=false, t24Name=INTEREST.BASIS, genOperation="IF ASSET.OR.LIAB = \'A\'  THEN AS.BASIS  ELSE LB.BASIS")
		AS_BASIS : mml:Integer
			@t24:i (align=L, inputBehaviour=N, maxLength=3, core=false, t24Name=AS.BASIS, genOperation="SUBR(\"ENQ.TRANS\",\"SWAP\",CONTRACT.ID,\"AS.BASIS\")")
		LB_BASIS : mml:Integer
			@t24:i (align=L, inputBehaviour=N, maxLength=3, core=false, t24Name=LB.BASIS, genOperation="SUBR(\"ENQ.TRANS\",\"SWAP\",CONTRACT.ID,\"LB.BASIS\")")
		PRODCAT : mml:Integer
			@i18n:Label (en="Deal Type")
			@t24:i (align=R, inputBehaviour=N, maxLength=5, core=false, t24Name=PRODCAT, genOperation="CONTRACT.ID>SWAP>PRODCAT")
		CUSTOMER_NO : mml:Integer
			@i18n:Label (en="Customer No")
			@i18n:Tooltip (en="Identifies the Customer for the transaction.")
			@t24:i (align=R, inputBehaviour=N, maxLength=10, core=false, t24Name=CUSTOMER.NO, genOperation="CONTRACT.ID>SWAP>CUSTOMER")
		PRODCCY : mml:Integer
			@i18n:Label (en="Product Currency")
			@t24:i (align=L, inputBehaviour=N, maxLength=3, core=false, t24Name=PRODCCY, genOperation=CURRENCY)
	}
	SWAP_BALANCES__PRINCIPAL
	{
		/* Defines the principal amount outstanding by date (PRIN.DATE) for the associated Swap leg.
Records the principal balance of the leg at each stage throughout the contract life.
Each time the principal balance changes on the leg, it will be recorded in a new multivalue in this field.
This field therefore consists of a number of multivalues, each recording the principal balance on the leg at various times throughout its life.  The date on which each amount became effective is stored in the associated multivalue of the PRIN.DATE field.
Information is stored in this field in reverse chronological order, ie. the current principal balance will always be found in the first multivalue.
This field is a no-input field and is automatically updated by the Swaps system.
Validation Rules
1 to 19 Numeric characters (Type AMT). Multivalue field associated with field PRIN.DATE. (No-input field). */
		PRINCIPAL : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=3.0, core=true, t24Name=PRINCIPAL, typeModifiers="1")
		/* Stores the dates on which the principal balance amounts in the field PRINCIPAL became effective.
Whenever the principal balance for the leg on the contract changes, the new balance is recorded in the associated PRINCIPAL field.  The PRIN.DATE field records the date on which each of these balances became effective.
Information is stored in these fields in reverse chronological order, ie. the most recent effective date will always be found in the first multivalue.
This field is a no-input field and is automatically updated by the Swaps system.
Validation Rules
11 Character standard date field. Multivalue field associated with field PRINCIPAL. (No-input field). */
		PRIN_DATE : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=4.0, core=true, t24Name=PRIN.DATE)
	}
	SWAP_BALANCES__EFFECTIVE_DATE
	{
		/* Stores the dates on which the interest rates in the INTEREST.RATE field became effective.
Whenever the interest rate on associated Swap leg changes, the new rate is recorded in the associated INTEREST.RATE field. The EFFECTIVE.DATE field records the date on which each of these rates became effective.
Information is stored in these fields in reverse chronological order, ie. the most recent effective date will always be found in the first multivalue.
This field is a no-input field and is updated automatically by the Swaps system.
Validation Rules
11 Character standard date field. Multivalued field associated with fields INTEREST.KEY and INTEREST.RATE and MKT.INT.RATE. (No-input field). */
		EFFECTIVE_DATE : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=5.0, core=true, t24Name=EFFECTIVE.DATE)
		/* Records the interest rate keys used on the associated effective dates (EFFECTIVE.DATE).
Each time the interest key changes on the Swap leg it will be recorded in a new multivalue in this field.
This field therefore consists of a number of multivalues, each recording the interest key applicable to the associated leg at various times throughout the life of the contract.  The date on which each interest key was effective is stored in the associated multivalue of the EFFECTIVE.DATE field.
Information is stored in this field in reverse chronological order, ie. the current interest rate key will always be found in the first multivalue.
This key is linked to the file MARKET.RATE.TEXT.
This field is a no-input field and is automatically updated by the Swaps system.
Validation Rules
1 to 4 Numeric characters. Multivalued field associated with fields EFFECTIVE.DATE, MKT.INT.RATE and INTEREST.RATE. (No-input field). */
		INTEREST_KEY : mml:Integer
			@t24:i (align=R, inputBehaviour=I, maxLength=6, sysNumber=6.0, core=true, t24Name=INTEREST.KEY)
		/* Records the interest rates applied to the portion of the outstanding principal specified in the field PRINCIPAL.
Each time the interest rate changes on the Swap leg it will be recorded in a new multivalue in this field.
This field therefore consists of a number of multivalues, each recording the interest rate applicable to the associated leg at various times throughout the life of the contract.  The date on which each rate was effective is stored in the associated multivalue of the EFFECTIVE.DATE field.
Information is stored in this field in reverse chronological order, ie. the current interest rate will always be found in the first multivalue.
The is a no-input field and is updated automatically by the Swaps system.
Validation Rules
1 to 11 Numeric character rate field. Multivalued field associated with fields EFFECTIVE.DATE, INTEREST.KEY and MKT.INT.RATE. (No-input field). */
		INTEREST_RATE : T24BusinessTypes:R
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=7.0, core=true, t24Name=INTEREST.RATE)
		/* It contains the Market Exchange Rate of a Leg. Which is difference of Current.rate and the Trsy.Rate

1 to 11 Numeric character rate field. Multivalued field associated with fields EFFECTIVE.DATE and INTEREST.KEY and INTEREST.RATE.

Validation Rules:
Fields EFFECTIVE.DATE to MKT.INT.RATE form a related multi value set.
Standard T24 rate field.
A maximum of 11 characters may be entered. */
		MKT_INT_RATE : T24BusinessTypes:R
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=8.0, core=true, t24Name=MKT.INT.RATE)
	}
	SWAP_BALANCES__ACCR_FROM_DATE
	{
		/* Identifies the date from which interest is accrued for the multivalue accrual group in question.  Ie. the start of the accrual period.
The set of fields ACCR.FROM.DATE to MKT.ACT.AMT form the multivalue set of interest accrual fields.  These fields record all details of interest accrued on the swap leg.  A new group is created each time one of the accrual parameters, ie. outstanding principal or interest rate changes or a month end has been reached.
This field records the dates from which interest accruals commence for each of the accrual groups.
Information is stored in these fields in reverse chronological order, ie. the most recent accrual details will always be found in the first multivalue of each field.
Validation Rules
11 Numeric characters (standard date format). Multivalued field associated with fields ACCR.FROM.DATE to ACCR.ACT.AMT. (No-input field). */
		ACCR_FROM_DATE : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=15.0, core=true, t24Name=ACCR.FROM.DATE)
		/* Identifies the end date upto which interest is accrued for the multivalue accrual group in question.  Ie. The end of accrual period.
The set of fields ACCR.FROM.DATE to MKT.ACT.AMT form the multivalue set of interest accrual fields.  These fields record all details of the interest accrued on the associated swap leg.
This field records the dates upto which interest accruals end for each of the accrual groups.
Information is stored in these fields in reverse chronological order, ie. the most recent accrual details will always be found in the first multivalue of each field.
Validation Rules
11 Numeric characters (Standard date format). Multivalue field associated with fields ACCR.FROM.DATE to ACCR.ACT.AMT. (No-input field). */
		ACCR_TO_DATE : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=16.0, core=true, t24Name=ACCR.TO.DATE)
		/* Identifies the number of days that interest has been accrued in the interest period for the multivalue accrual group in question.
The set of fields ACCR.FROM.DATE to MKT.ACT.AMT form the multivalue set of interest accrual fields.  These fields record all details of the interest accrued on the Swap leg.
This field records the number of days that interest has been accrued for each of the accrual groups.
Information is stored in these fields in reverse chronological order, ie. the most recent accrual details will always be found in the first multivalue of each field.
Validation Rules
1 to 4 Numeric characters. Multivalue field associated with fields ACCR.FROM.DATE to ACCR.ACT.AMT. (No-input field). */
		ACCR_DAYS : mml:Integer
			@t24:i (align=R, inputBehaviour=I, maxLength=4, sysNumber=17.0, core=true, t24Name=ACCR.DAYS)
		/* Identifies the principal amount effective during the accrual for the multivalue accrual group in question.
The set of fields ACCR.FROM.DATE to MKT.ACT.AMT form the multivalue set of interest accrual fields.  These fields record all details of interest accrued on the Swap contract leg.
This field records the outstanding leg principal on which interest was accrued for each of the accrual groups.
Information is stored in these fields in reverse chronological order, ie. the most recent accrual details will always be found in the first multivalue of each field.
Validation Rules
1-19 Numeric character amount field. Multivalue field associated with fields ACCR.FROM.DATE to ACCR.ACT.AMT. (No-input field). */
		ACCR_PRIN : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=18.0, core=true, t24Name=ACCR.PRIN)
		/* Identifies the effective interest rate during the accrual period for the multivalue accrual group in question.
The set of fields ACCR.FROM.DATE to MKT.ACT.AMT form the multivalue set of interest accrual fields.  These fields record all details of interest accrued on the Swap leg.
This field records the interest rates which have been applied for each of the accrual groups.
Information is stored in these fields in reverse chronological order, ie. the most recent accrual details will always be found in the first multivalue of each field.
Validation Rules
1 - 11 Numeric characters. Multivalue field associated with fields ACCR.FROM.DATE to ACCR.ACT.AMT. (No-input field). */
		ACCR_RATE : T24BusinessTypes:R
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=19.0, core=true, t24Name=ACCR.RATE)
		/* This field contains the amount of interest accrued for the multivalue accrual group in question, rounded to the number of decimal places of the currency of the leg.
The set of fields ACCR.FROM.DATE to MKT.ACT.AMT form the multivalue set of interest accrual fields.  These fields record all details of interest accrued on the swap leg.
This amount is accumulated for each interest period and recorded in the INTEREST.AMOUNT field which then determines the amount of interest to be paid/received at the end of the interest period.
The un-rounded amounts are held in the associated ACCR.ACT.AMT field.
Information is stored in these fields in reverse chronological order, ie. the most recent accrual details will always be found in the first multivalue of each field.
Validation Rules
1 to 19 Numeric character Amount field. Multivalue field associated with fields ACCR.FROM.DATE to ACCR.ACT.AMT. (No-input field). */
		ACCR_AMT : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=20.0, core=true, t24Name=ACCR.AMT, typeModifiers="1")
		/* This field contains the amount of interest accrued, unrounded, for the multivalue accrual group in question.
The set of fields ACCR.FROM.DATE to MKT.ACT.AMT form the multivalue set of interest accrual fields.  These fields record all details of interest accrued on the swap leg.
This field records the amount of interest accrued for each of the multivalue accrual groups.  This amount is unrounded.  The rounded accrued amount is held in the associated field ACCR.AMT.
Information is stored in these fields in reverse chronological order, ie. the most recent accrual details will always be found in the first multivalue of each field.
Validation Rules
1 to 19 Numeric character Amount field. Multivalue field associated with fields ACCR.FROM.DATE to ACCR.ACT.AMT. (No-input field). */
		ACCR_ACT_AMT : mml:Integer
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=21.0, core=true, t24Name=ACCR.ACT.AMT)
		/* Identifies the effective interest rate for market exchange during the accrual period for the multivalue accrual group in question.

The set of fields ACCR.FROM.DATE to MKT.ACT.AMT form the multivalue set of interest accrual fields. These fields record all details of interest accrued for market exchange on the Swap leg.

This field records the interest rates which have been applied for each of the accrual groups.

Information is stored in these fields in reverse chronological order, ie. the most recent accrual details will always be found in the first multivalue of each field.

Validation Rules

1 - 11 Numeric characters. Multivalue field associated with fields ACCR.FROM.DATE to MKT.ACT.AMT. (No-input field).
Fields ACCR.FROM.DATE to MKT.ACT.AMT form a related multi value set.
A maximum of 19 characters may be entered. */
		MKT_ACCR_RATE : T24BusinessTypes:R
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=22.0, core=true, t24Name=MKT.ACCR.RATE)
		/* It contains the Market Exchange Accrual amount for a period.

The set of fields ACCR.FROM.DATE to MKT.ACT.AMT form the multivalue set of interest accrual fields. These fields record all details of market exchange interest accrued on the swap leg.

This amount is accumulated for each interest period and recorded in the MKT.INT.AMOUNT field which then determines the amount of interest to be paid/received at the end of the interest period.

The un-rounded amounts are held in the associated MKT.ACT.AMT field.

Information is stored in these fields in reverse chronological order, ie. the most recent accrual details will always be found in the first multivalue of each field.

Validation Rules

1 to 19 Numeric character Amount field.
Multivalue field associated with fields ACCR.FROM.DATE to MKT.ACT.AMT. (No-input field). */
		MKT_ACCR_AMT : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=23.0, core=true, t24Name=MKT.ACCR.AMT, typeModifiers="1")
		/* This field contains the amount of interest for market exchange accrued, unrounded, for the multivalue accrual group in question.

The set of fields ACCR.FROM.DATE to MKT.ACT.AMT form the multivalue set of interest accrual fields. These fields record all details of interest accrued on the swap leg.

This field records the market exchange amount of interest accrued for each of the multivalue accrual groups. This amount is unrounded. The rounded accrued amount is held in the associated field MKT.ACCR.AMT.
Validation Rules

1 to 19 Numeric character Amount field.
Multivalue field associated with fields ACCR.FROM.DATE to MKT.ACT.AMT. (No-input field). */
		MKT_ACT_AMT : mml:Integer
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=24.0, core=true, t24Name=MKT.ACT.AMT)
	}
	SWAP_BALANCES__AMORT_SCHEDULE
	{
		/* Identifies the schedule type
Updated only for the schedules PM and RV.

Validation Rules
(1) 1 to 12 Alphanumeric characters.
Multivalued field associated with fields AMORT.SCHEDULE to AMORT.TO.DATE.
(No-input field). */
		AMORT_SCHEDULE : T24BusinessTypes:A
			@t24:i (align=L, inputBehaviour=I, maxLength=12, sysNumber=25.0, core=true, t24Name=AMORT.SCHEDULE)
		/* Contains the original premium/discount amount for the corresponding leg as inputted in SWAP.
This field is updated only when SWAP contains a PM or RV schedule.

Validation Rules
(1) 1 to 19 Numeric character (standard amount field).
Multivalued field associated with field AMORT.SCHEDULE.
(No-input field). */
		AMORT_AMOUNT : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=26.0, core=true, t24Name=AMORT.AMOUNT, typeModifiers="1")
		/* Identifies the start date of the Amortisation period.
This field is updated only for PM  or RV schedules.

Validation Rules
(1) 11 Character standard date field.
Multivalue field associated with field AMORT.SCHEDULE.
(No-input field). */
		AMORT_START : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=27.0, core=true, t24Name=AMORT.START)
		/* Identifies the end date of the Amortisation period as inputted in SWAP contract field
LB/AS.AMORT.DATE for the corresponding leg.
This field is updated only for PM or RV schedules.

Validation Rules
(1) 11 Character standard date field.
Multivalue field associated with field AMORT.SCHEDULE.
(No-input field) */
		AMORT_END : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=28.0, core=true, t24Name=AMORT.END)
		/* This field contains the amount that has been amortised so far.
This field is updated only for PM or RV schedules.

Validation Rules
(1) 1 to 19 Numeric character (standard amount field).
Multivalued field associated with field AMORT.SCHEDULE.
(No-input field). */
		AMORT_TO_DATE : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=29.0, core=true, t24Name=AMORT.TO.DATE)
	}
	SWAP_BALANCES__SCHEDULE_TYPE
	{
		/* Identifies the schedule type for the multivalue schedules past group in question.
The set of fields SCHEDULE.TYPE to VALUE.DATE form the multivalue set of Swap schedules past fields.  These fields record all details of schedules that have been processed on the Swap leg.  A new group is created each time a schedule is processed on the Swap contract.
This field records the types of schedules processed.
Information is stored in these fields in reverse chronological order, ie. the most recent schedule processed details will always be found in the first multivalue of each field.
Validation Rules
1 to 12 Alphanumeric characters. Multivalued field associated with fields SCHEDULE.TYPE to VALUE.DATE. (No-input field). */
		SCHEDULE_TYPE -> SW_Schedules:SWAP_SCHEDULE_TYPE
			@t24:i (align=L, inputBehaviour=I, maxLength=12, sysNumber=30.0, core=true, businessType=A, t24Name=SCHEDULE.TYPE)
		/* Identifies the charge code for the multivalue schedules past group in question.
The set of fields SCHEDULE.TYPE to VALUE.DATE form the multivalue set of Swap schedules past fields.  These fields record all details of schedules that have been processed on the Swap leg.  A new group is created each time a schedule is processed on the Swap contract.
This field records the charge codes of schedules processed.
Information is stored in these fields in reverse chronological order, ie. the most recent schedule processed details will always be found in the first multivalue of each field.
Validation Rules
1 to 12 Alphanumeric characters. Multivalued field associated with fields SCHEDULE.TYPE to VALUE.DATE. (No-input field). */
		CHARGE_CODE : T24BusinessTypes:CHG
			@t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=31.0, core=true, t24Name=CHARGE.CODE, typeModifiers="CHGCOMDTAX")
		/* Identifies the schedule date of the schedule processed for the multivalue schedules past group in question.
The set of fields SCHEDULE.TYPE to VALUE.DATE form the multivalue set of Swap schedules past fields.  These fields record all details of schedules that have been processed on the Swap leg.  A new group is created each time a schedule is processed on the Swap contract.
This field records the schedule dates of the schedules processed.  These are not the dates that the schedules were actual processed.  This information in held in the associated field PROCESS.DATE.
Information is stored in these fields in reverse chronological order, ie. the most recent schedule processed details will always be found in the first multivalue of each field.
Validation Rules
11 Character standard date field. Multivalued field associated with fields SCHEDULE.TYPE to VALUE.DATE. (No-input field). */
		SCHEDULE_DATE : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=32.0, core=true, t24Name=SCHEDULE.DATE)
		/* Identifies the date that the schedule was actually processed for the multivalue schedules past group in question.
The set of fields SCHEDULE.TYPE to VALUE.DATE form the multivalue set of Swap schedules past fields.  These fields record all details of schedules that have been processed on the Swap leg.  A new group is created each time a schedule is processed on the Swap contract.
This field records the process dates of the schedules processed.  The associated schedule dates are held in the field SCHEDULE.DATE.
Information is stored in these fieldsin reverse chronological order, ie. the most recent schedule processed details will always be found in the first multivalue of each field.
Validation Rules
11 Character standard date format. Multivalued field assocaited with fields SCHEDULE.TYPE to VALUE.DATE. (No-input field). */
		PROCESS_DATE : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=33.0, core=true, t24Name=PROCESS.DATE)
		/* Identifies the interest period start date of the schedule processed for the multivalue schedules past group in question.
The set of fields SCHEDULE.TYPE to VALUE.DATE form the multivalue set of Swap schedules past fields.  These fields record all details of schedules that have been processed on the Swap leg.  A new group is created each time a schedule is processed on the Swap contract.
This field records the interest period start dates of the schedules processed.
Information is stored in these fields in reverse chronological order, ie. the most recent schedule processed details will always be found in the first multivalue of each field.
Validation Rules
11 Character standard date field. Multivalued field associated with fields SCHEDULE.TYPE to VALUE.DATE. (No-input field). */
		PERIOD_START : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=34.0, core=true, t24Name=PERIOD.START)
		/* Identifies the interest period end date of the schedule processed for the multivalue schedules past group in question.
The set of fields SCHEDULE.TYPE to VALUE.DATE form the multivalue set of Swap schedules past fields.  These fields record all details of schedules that have been processed on the Swap leg.  A new group is created each time a schedule is processed on the Swap contract.
This field records the interest period end dates of the schedules processed.
Information is stored in these fields in reverse chronological order, ie. the most recent schedule processed details will always be found in the first multivalue of each field.
Validation Rules
11 Character standard date field. Multivalued field associated with fields SCHEDULE.TYPE to VALUE.DATE. (No-input field). */
		PERIOD_END : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=35.0, core=true, t24Name=PERIOD.END)
		/* Identifies the interest effective date of the schedule processed for the multivalue schedules past group in question.
The set of fields SCHEDULE.TYPE to VALUE.DATE form the multivalue set of Swap schedules past fields.  These fields record all details of schedules that have been processed on the Swap leg.  A new group is created each time a schedule is processed on the Swap contract.
This field records the dates that the schedules were effective for interest accruals.
Information is stored in these fields in reverse chronological order, ie. the most recent schedule processed details will always be found in the first multivalue of each field.
Validation Rules
11 Character standard date field. Multivalued field associated with fields SCHEDULE.TYPE to VALUE.DATE. (No-input field). */
		SCHED_EFF_DATE : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=36.0, core=true, t24Name=SCHED.EFF.DATE)
		/* Identifies the schedule currency amount posted for the multivalue schedules past group in question.
The set of fields SCHEDULE.TYPE to VALUE.DATE form the multivalue set of Swap schedules past fields.  These fields record all details of schedules that have been processed on the Swap leg.  A new group is created each time a schedule is processed on the Swap contract.
This field records the schedule amounts posted.  If the amount is not of local currency, then its' local equivalent is held in the associated field LCL.AMOUNT.
Information is stored in these fields in reverse chronological order, ie. the most recent schedule processed details will always be found in the first multivalue of each field.
Validation Rules
1 to 19 Numeric character (standard amount field). Multivalued field associated with fields SCHEDULE.TYPE to VALUE.DATE. (No-input field). */
		CCY_AMOUNT : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=37.0, core=true, t24Name=CCY.AMOUNT, typeModifiers="1")
		/* Identifies the schedule currency amount for market exchange, posted for the multivalue schedules past group in question.

This field records the schedule amounts posted for market exchange.

Information is stored in these fields in reverse chronological order, ie. the most recent schedule processed details will always be found in the first multivalue of each field.

Validation Rules

1 to 19 Numeric character (standard amount field).
Multivalued field associated with fields SCHEDULE.TYPE to VALUE.DATE. (No-input field). */
		MKT_CCY_AMT : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=38.0, core=true, t24Name=MKT.CCY.AMT, typeModifiers="1")
		/* Identifies the rate of the RR schedule processed for the multivalue schedules past group in question.
The set of fields SCHEDULE.TYPE to VALUE.DATE form the multivalue set of Swap schedules past fields.  These fields record all details of schedules that have been processed on the Swap leg.  A new group is created each time a schedule is processed on the Swap contract.
This field records the rates of the RR schedules processed.
Information is stored in these fields in reverse chronological order, ie. the most recent schedule processed details will always be found in the first multivalue of each field.
Validation Rules
1-11 Numeric character standard rate field. Multivalued field associated with fields SCHEDULE.TYPE to VALUE.DATE. (No-input field). */
		RESET_RATE : T24BusinessTypes:R
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=39.0, core=true, t24Name=RESET.RATE)
		/* Identifies the schedule local currency amount posted for the multivalue schedules past group in question.
The set of fields SCHEDULE.TYPE to VALUE.DATE form the multivalue set of Swap schedules past fields.  These fields record all details of schedules that have been processed on the Swap leg.  A new group is created each time a schedule is processed on the Swap contract.
This field records the schedule local currency amounts posted.  If the Swap leg is not in local currency, then its' currency equivalent is held in the associated field CCY.AMOUNT.
Information is stored in these fields in reverse chronological order, ie. the most recent schedule processed details will always be found in the first multivalue of each field.
Validation Rules
1 to 19 Numeric character (standard amount field). Multivalued field associated with fields SCHEDULE.TYPE to VALUE.DATE. (No-input field). */
		LCL_AMOUNT : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=40.0, core=true, t24Name=LCL.AMOUNT, typeModifiers="USD")
		/* Identifies the currency amount difference in the 'IS' and 'AP' schedules.  For 'IS' schedule, the difference is between the original principal and the issue price whereas for 'AP' schedule the difference is between the repayment amount and the interest amount.
The set of fields SCHEDULE.TYPE to VALUE.DATE form the multivalue set of Swap schedules past fields.  These fields record all details of schedules that have been processed on the Swap leg.  A new group is created each time a schedule is processed on the Swap contract.
Information is stored in these fields in reverse chronological order, ie. the most recent schedule processed details will always be found in the first multivalue of each field.
Validation Rules
1 to 19 Numeric character (standard amount field). Multivalued field associated with fields SCHEDULE.TYPE to VALUE.DATE. (No-input field). */
		AMOUNT_DIFF : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=41.0, core=true, t24Name=AMOUNT.DIFF, typeModifiers="1")
		/* Identifies the value date of the schedule entry processed for the multivalue schedules past group in question.
The set of fields SCHEDULE.TYPE to VALUE.DATE form the multivalue set of Swap schedules past fields.  These fields record all details of schedules that have been processed on the Swap leg.  A new group is created each time a schedule is processed on the Swap contract.
This field records the schedule value dates of the schedules processed.
Information is stored in these fields in reverse chronological order, ie. the most recent schedule processed details will always be found in the first multivalue of each field.
Validation Rules
11 Character standard date field. Multivalued field associated with fields SCHEDULE.TYPE to VALUE.DATE. (No-input field). */
		VALUE_DATE : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=42.0, core=true, t24Name=VALUE.DATE)
	}
	SWAP_BALANCES__POSITION_DATE
	{
		/* Fields POSITION.DATE to  form a related multi value set. Standard T24 date field.
Validation Rules
A maximum of 11 characters may be entered. */
		POSITION_DATE : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=52.0, core=true, t24Name=POSITION.DATE)
		/* Fields POSITION.DATE to  form a related multi value set.
Validation Rules
A maximum of 19 characters may be entered. */
		POSITION_FCY : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=53.0, core=true, t24Name=POSITION.FCY, typeModifiers="1")
		/* Fields POSITION.DATE to  form a related multi value set.
Validation Rules
A maximum of 19 characters may be entered. */
		POSITION_LCY : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=54.0, core=true, t24Name=POSITION.LCY, typeModifiers="USD")
	}
	/* The SWAP.BALANCES.HIST file stores the reversed versions of Swap balance records (SWAP.BALANCES).
For further information with regard to this file and its' associated fields, please see SWAP.BALANCES helptext. */
	SWAP_BALANCES_HIST
		@t24:i (t24Name=SWAP.BALANCES.HIST, level=F, type=L, allowedFunctions="L P S", localRefAllowed=false)
	{
		/* Standard T24 alphanumeric field.
Validation Rules
A maximum of 30 characters may be entered. */
		CONTRACT_NUMBER -> SW_Contract:SWAP_BALANCES_HIST PK required
			@i18n:Label (en="Contract Number")
			@t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=0.0, core=true, businessType=A, t24Name=CONTRACT.NUMBER)
		/* Standard T24 currency field.
Validation Rules
A maximum of 3 characters may be entered. */
		CURRENCY : T24BusinessTypes:CCY
			@i18n:Label (en=Ccy)
			@t24:i (align=L, inputBehaviour=I, maxLength=3, sysNumber=1.0, core=true, t24Name=CURRENCY)
		/* Validation Rules
A maximum of 2 characters may be entered. */
		NOTIONAL : T24BusinessTypes:A
			@t24:i (align=L, inputBehaviour=I, maxLength=2, sysNumber=2.0, core=true, t24Name=NOTIONAL)
		PRINCIPAL -> byValue SW_Contract:SWAP_BALANCES_HIST__PRINCIPAL *
		EFFECTIVE_DATE -> byValue SW_Contract:SWAP_BALANCES_HIST__EFFECTIVE_DATE *
		/* Validation Rules
A maximum of 19 characters may be entered. */
		INTEREST_AMOUNT : T24BusinessTypes:AMT
			@i18n:Label (en="Interest Amt")
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=9.0, core=true, t24Name=INTEREST.AMOUNT, typeModifiers="1")
		MKT_INT_AMOUNT : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=10.0, core=true, t24Name=MKT.INT.AMOUNT, typeModifiers="1")
		/* Standard T24 date field.
Validation Rules
A maximum of 11 characters may be entered. */
		START_INT_PERIOD : T24BusinessTypes:D
			@i18n:Label (en="Start Int Period")
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=11.0, core=true, t24Name=START.INT.PERIOD)
		/* Standard T24 date field.
Validation Rules
A maximum of 11 characters may be entered. */
		END_INT_PERIOD : T24BusinessTypes:D
			@i18n:Label (en="End Int Period")
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=12.0, core=true, t24Name=END.INT.PERIOD)
		TOT_INT_AMT : T24BusinessTypes:AMT
			@i18n:Label (en="Total Intt Amt")
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=13.0, core=true, t24Name=TOT.INT.AMT, typeModifiers="1")
		TOT_MKT_INT_AMT : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=14.0, core=true, t24Name=TOT.MKT.INT.AMT, typeModifiers="1")
		ACCR_FROM_DATE -> byValue SW_Contract:SWAP_BALANCES_HIST__ACCR_FROM_DATE *
		AMORT_SCHEDULE -> byValue SW_Contract:SWAP_BALANCES_HIST__AMORT_SCHEDULE *
		SCHEDULE_TYPE -> byValue SW_Contract:SWAP_BALANCES_HIST__SCHEDULE_TYPE *
		/* Standard T24 date field.
Validation Rules
A maximum of 11 characters may be entered. */
		CRB_VALUE_DATE : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=43.0, core=true, t24Name=CRB.VALUE.DATE)
		/* Standard T24 date field.
Validation Rules
A maximum of 11 characters may be entered. */
		CRB_MATURITY_DATE : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=44.0, core=true, t24Name=CRB.MATURITY.DATE)
		/* Standard T24 date field.
Validation Rules
A maximum of 11 characters may be entered. */
		CRB_INTEREST_DATE : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=45.0, core=true, t24Name=CRB.INTEREST.DATE)
		/* Standard T24 alphanumeric field.
Validation Rules
A maximum of 16 characters may be entered. */
		CONSOL_KEY : T24BusinessTypes:A
			@i18n:Label (en="Consol Key")
			@t24:i (align=L, inputBehaviour=I, maxLength=16, sysNumber=46.0, core=true, t24Name=CONSOL.KEY)
		/* This field will only become populated when the leg of the swap contract has undergone the currency revaluation processing. The leg of the swap contract must meet all of the following criteria to undergo the currency revaluation processing;
1) The contract must be OFF balance sheet.   2) The leg must be non notional.   3) The currency of the leg must not be the local currency.
When a leg of a swap contract has undergone the currency revaluation processing, this field will hold the exchange rate which was applicable to the currency at the time the leg became non notional.
Validation Rules
Standard T24 rate field. */
		INITIAL_XRATE : T24BusinessTypes:R
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=47.0, core=true, t24Name=INITIAL.XRATE)
		/* It holds the principal outstanding in local currency.
All principal movements will update this field.
Validation Rules
1-19 type AMT (standard amount format) characters plus a decimal point. */
		OUTS_PRIN_LCY : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=48.0, core=true, t24Name=OUTS.PRIN.LCY, typeModifiers="USD")
		/* This field holds the net amount which has been posted as unrealised P&L in local currency.
The amount is derived by the formula:- Principal outstanding * (current.xrate - initial.xrate)
Validation Rules
1-19 type AMT (standard amount format) characters plus a decimal point. */
		CCY_REVAL_PL : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=49.0, core=true, t24Name=CCY.REVAL.PL, typeModifiers="USD")
		/* Validation Rules
A maximum of 19 characters may be entered. */
		NPV : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=50.0, core=true, t24Name=NPV, typeModifiers="1")
		/* Validation Rules
A maximum of 19 characters may be entered. */
		NPV_LCY : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=51.0, core=true, t24Name=NPV.LCY, typeModifiers="USD")
		POSITION_DATE -> byValue SW_Contract:SWAP_BALANCES_HIST__POSITION_DATE *
		CONF_SENT : mml:Integer
			@t24:i (align=R, inputBehaviour=N, maxLength=55, sysNumber=55.0, core=true, t24Name=CONF.SENT)
		OLD_OUTS_PRIN_LCY : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=56.0, core=true, t24Name=OLD.OUTS.PRIN.LCY)
		NPV_BEF_ACC_ADJ : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=57.0, core=true, t24Name=NPV.BEF.ACC.ADJ)
		NPV_BEF_ADJ_LCY : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=58.0, core=true, t24Name=NPV.BEF.ADJ.LCY)
		NPV_INC_TDY_IP : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=59.0, core=true, t24Name=NPV.INC.TDY.IP)
		NPV_INC_TDY_IPLCY : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=60.0, core=true, t24Name=NPV.INC.TDY.IPLCY)
		CCY_REVAL_FCY : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=61.0, core=true, t24Name=CCY.REVAL.FCY)
		CONTRACT_ID : mml:Integer
			@i18n:Label (en="Contract Id")
			@t24:i (align=R, inputBehaviour=N, maxLength=30, core=false, t24Name=CONTRACT.ID, genOperation="@ID[\'.\',1,1]")
		CONTRACT_FILE : mml:Integer
			@t24:i (align=L, inputBehaviour=N, maxLength=4, core=false, t24Name=CONTRACT.FILE, genOperation="\"SWAP\"")
		SECTOR : mml:Integer
			@i18n:Label (en=Sector)
			@i18n:Tooltip (en="Indicates the sector")
			@t24:i (align=R, inputBehaviour=N, maxLength=4, core=false, t24Name=SECTOR, genOperation="SUBR(\"ENQ.TRANS\",\"SWAP\",CONTRACT.ID,\"SECTOR\")")
		INDUSTRY : mml:Integer
			@i18n:Label (en=Industry)
			@i18n:Tooltip (en="Indicates the Industry")
			@t24:i (align=R, inputBehaviour=N, maxLength=4, core=false, t24Name=INDUSTRY, genOperation="SUBR(\"ENQ.TRANS\",\"SWAP\",CONTRACT.ID,\"INDUSTRY\")")
		ACCOUNT_OFFICER : mml:Integer
			@i18n:Label (en="Account Officer")
			@i18n:Tooltip (en="Indicates Account Officer responsible")
			@t24:i (align=R, inputBehaviour=N, maxLength=4, core=false, t24Name=ACCOUNT.OFFICER, genOperation="SUBR(\"ENQ.TRANS\",\"SWAP\",CONTRACT.ID, \"ACCOUNT.OFFICER\")")
		TARGET : mml:Integer
			@i18n:Label (en=Target)
			@t24:i (align=R, inputBehaviour=N, maxLength=4, core=false, t24Name=TARGET, genOperation="SUBR(\"ENQ.TRANS\",\"SWAP\",CONTRACT.ID,\"TARGET\")")
		PRODCAT : mml:Integer
			@i18n:Label (en="Deal Type")
			@t24:i (align=R, inputBehaviour=N, maxLength=5, core=false, t24Name=PRODCAT, genOperation="SUBR(\"ENQ.TRANS\",\"SWAP\",CONTRACT.ID, \"PRODUCT.CATEGORY\")")
		RESIDENCE : mml:Integer
			@i18n:Label (en=Residence)
			@t24:i (align=L, inputBehaviour=N, maxLength=2, core=false, t24Name=RESIDENCE, genOperation="SUBR(\"ENQ.TRANS\",\"SWAP\",CONTRACT.ID,\"RESIDENCE\")")
		ASSET_OR_LIAB : mml:Integer
			@t24:i (align=L, inputBehaviour=N, maxLength=3, core=false, t24Name=ASSET.OR.LIAB, genOperation="@ID[\'.\',2,1]")
		INTEREST_BASIS : mml:Integer
			@i18n:Label (en="Interest Basis")
			@i18n:Tooltip (en="Indicates the interest day basis")
			@t24:i (align=L, inputBehaviour=N, maxLength=3, core=false, t24Name=INTEREST.BASIS, genOperation="IF ASSET.OR.LIAB = \'A\'  THEN AS.BASIS  ELSE LB.BASIS")
		AS_BASIS : mml:Integer
			@t24:i (align=L, inputBehaviour=N, maxLength=3, core=false, t24Name=AS.BASIS, genOperation="SUBR(\"ENQ.TRANS\",\"SWAP\",CONTRACT.ID,\"AS.BASIS\")")
		LB_BASIS : mml:Integer
			@t24:i (align=L, inputBehaviour=N, maxLength=3, core=false, t24Name=LB.BASIS, genOperation="SUBR(\"ENQ.TRANS\",\"SWAP\",CONTRACT.ID,\"LB.BASIS\")")
		CUSTOMER_NO : mml:Integer
			@i18n:Label (en="Customer No")
			@i18n:Tooltip (en="Identifies the Customer for the transaction.")
			@t24:i (align=R, inputBehaviour=N, maxLength=10, core=false, t24Name=CUSTOMER.NO, genOperation="SUBR(\"ENQ.TRANS\",\"SWAP\",CONTRACT.ID,\"CUSTOMER\")")
		PRODCCY : mml:Integer
			@i18n:Label (en="Product Currency")
			@t24:i (align=L, inputBehaviour=N, maxLength=3, core=false, t24Name=PRODCCY, genOperation=CURRENCY)
	}
	SWAP_BALANCES_HIST__PRINCIPAL
	{
		/* Fields PRINCIPAL to PRIN.DATE form a related multi value set.
Validation Rules
A maximum of 19 characters may be entered. */
		PRINCIPAL : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=3.0, core=true, t24Name=PRINCIPAL, typeModifiers="1")
		/* Fields PRINCIPAL to PRIN.DATE form a related multi value set. Standard T24 date field.
Validation Rules
A maximum of 11 characters may be entered. */
		PRIN_DATE : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=4.0, core=true, t24Name=PRIN.DATE)
	}
	SWAP_BALANCES_HIST__EFFECTIVE_DATE
	{
		/* Fields EFFECTIVE.DATE to INTEREST.RATE form a related multi value set. Standard T24 date field.
Validation Rules
A maximum of 11 characters may be entered. */
		EFFECTIVE_DATE : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=5.0, core=true, t24Name=EFFECTIVE.DATE)
		/* Fields EFFECTIVE.DATE to INTEREST.RATE form a related multi value set. Standard T24 numeric field.
Validation Rules
A maximum of 4 characters may be entered. */
		INTEREST_KEY : mml:Integer
			@t24:i (align=R, inputBehaviour=I, maxLength=6, sysNumber=6.0, core=true, t24Name=INTEREST.KEY)
		/* Fields EFFECTIVE.DATE to INTEREST.RATE form a related multi value set. Standard T24 rate field.
Validation Rules
A maximum of 11 characters may be entered. */
		INTEREST_RATE : T24BusinessTypes:R
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=7.0, core=true, t24Name=INTEREST.RATE)
		MKT_INT_RATE : T24BusinessTypes:R
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=8.0, core=true, t24Name=MKT.INT.RATE)
	}
	SWAP_BALANCES_HIST__ACCR_FROM_DATE
	{
		/* Fields ACCR.FROM.DATE to ACCR.ACT.AMT form a related multi value set. Standard T24 date field.
Validation Rules
A maximum of 11 characters may be entered. */
		ACCR_FROM_DATE : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=15.0, core=true, t24Name=ACCR.FROM.DATE)
		/* Fields ACCR.FROM.DATE to ACCR.ACT.AMT form a related multi value set. Standard T24 date field.
Validation Rules
A maximum of 11 characters may be entered. */
		ACCR_TO_DATE : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=16.0, core=true, t24Name=ACCR.TO.DATE)
		/* Fields ACCR.FROM.DATE to ACCR.ACT.AMT form a related multi value set. Standard T24 numeric field.
Validation Rules
A maximum of 4 characters may be entered. */
		ACCR_DAYS : mml:Integer
			@t24:i (align=R, inputBehaviour=I, maxLength=4, sysNumber=17.0, core=true, t24Name=ACCR.DAYS)
		/* Fields ACCR.FROM.DATE to ACCR.ACT.AMT form a related multi value set. Standard T24 amount field.
Validation Rules
A maximum of 19 characters may be entered. */
		ACCR_PRIN : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=18.0, core=true, t24Name=ACCR.PRIN)
		/* Fields ACCR.FROM.DATE to ACCR.ACT.AMT form a related multi value set. Standard T24 rate field.
Validation Rules
A maximum of 11 characters may be entered. */
		ACCR_RATE : T24BusinessTypes:R
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=19.0, core=true, t24Name=ACCR.RATE)
		/* Fields ACCR.FROM.DATE to ACCR.ACT.AMT form a related multi value set.
Validation Rules
A maximum of 19 characters may be entered. */
		ACCR_AMT : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=20.0, core=true, t24Name=ACCR.AMT, typeModifiers="1")
		/* Fields ACCR.FROM.DATE to ACCR.ACT.AMT form a related multi value set. Standard T24 numeric field.
Validation Rules
A maximum of 19 characters may be entered. */
		ACCR_ACT_AMT : mml:Integer
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=21.0, core=true, t24Name=ACCR.ACT.AMT)
		MKT_ACCR_RATE : T24BusinessTypes:R
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=22.0, core=true, t24Name=MKT.ACCR.RATE)
		MKT_ACCR_AMT : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=23.0, core=true, t24Name=MKT.ACCR.AMT, typeModifiers="1")
		MKT_ACT_AMT : mml:Integer
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=24.0, core=true, t24Name=MKT.ACT.AMT)
	}
	SWAP_BALANCES_HIST__AMORT_SCHEDULE
	{
		/* Fields AMORT.SCHEDULE to AMORT.TO.DATE form a related multi value set. Standard T24 alphanumeric field.
Validation Rules
A maximum of 12 characters may be entered. */
		AMORT_SCHEDULE : T24BusinessTypes:A
			@t24:i (align=L, inputBehaviour=I, maxLength=12, sysNumber=25.0, core=true, t24Name=AMORT.SCHEDULE)
		/* Fields AMORT.SCHEDULE to AMORT.TO.DATE form a related multi value set.
Validation Rules
A maximum of 19 characters may be entered. */
		AMORT_AMOUNT : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=26.0, core=true, t24Name=AMORT.AMOUNT, typeModifiers="1")
		/* Fields AMORT.SCHEDULE to AMORT.TO.DATE form a related multi value set. Standard T24 date field.
Validation Rules
A maximum of 11 characters may be entered. */
		AMORT_START : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=27.0, core=true, t24Name=AMORT.START)
		/* Fields AMORT.SCHEDULE to AMORT.TO.DATE form a related multi value set. Standard T24 date field.
Validation Rules
A maximum of 11 characters may be entered. */
		AMORT_END : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=28.0, core=true, t24Name=AMORT.END)
		/* Fields AMORT.SCHEDULE to AMORT.TO.DATE form a related multi value set. Standard T24 amount field.
Validation Rules
A maximum of 19 characters may be entered. */
		AMORT_TO_DATE : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=29.0, core=true, t24Name=AMORT.TO.DATE)
	}
	SWAP_BALANCES_HIST__SCHEDULE_TYPE
	{
		/* Fields SCHEDULE.TYPE to VALUE.DATE form a related multi value set. Standard T24 alphanumeric field.
Validation Rules
A maximum of 12 characters may be entered.
Must be the key to a valid entry on the SWAP.SCHEDULE.TYPE file. */
		SCHEDULE_TYPE -> SW_Schedules:SWAP_SCHEDULE_TYPE
			@t24:i (align=L, inputBehaviour=I, maxLength=12, sysNumber=30.0, core=true, businessType=A, t24Name=SCHEDULE.TYPE)
		/* Fields SCHEDULE.TYPE to VALUE.DATE form a related multi value set.
Validation Rules
A maximum of 12 characters may be entered. */
		CHARGE_CODE : T24BusinessTypes:CHG
			@t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=31.0, core=true, t24Name=CHARGE.CODE, typeModifiers="CHGCOMDTAX")
		/* Fields SCHEDULE.TYPE to VALUE.DATE form a related multi value set. Standard T24 date field.
Validation Rules
A maximum of 11 characters may be entered. */
		SCHEDULE_DATE : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=32.0, core=true, t24Name=SCHEDULE.DATE)
		/* Fields SCHEDULE.TYPE to VALUE.DATE form a related multi value set. Standard T24 date field.
Validation Rules
A maximum of 11 characters may be entered. */
		PROCESS_DATE : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=33.0, core=true, t24Name=PROCESS.DATE)
		/* Fields SCHEDULE.TYPE to VALUE.DATE form a related multi value set. Standard T24 date field.
Validation Rules
A maximum of 11 characters may be entered. */
		PERIOD_START : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=34.0, core=true, t24Name=PERIOD.START)
		/* Fields SCHEDULE.TYPE to VALUE.DATE form a related multi value set. Standard T24 date field.
Validation Rules
A maximum of 11 characters may be entered. */
		PERIOD_END : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=35.0, core=true, t24Name=PERIOD.END)
		/* Fields SCHEDULE.TYPE to VALUE.DATE form a related multi value set. Standard T24 date field.
Validation Rules
A maximum of 11 characters may be entered. */
		SCHED_EFF_DATE : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=36.0, core=true, t24Name=SCHED.EFF.DATE)
		/* Fields SCHEDULE.TYPE to VALUE.DATE form a related multi value set.
Validation Rules
A maximum of 19 characters may be entered. */
		CCY_AMOUNT : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=37.0, core=true, t24Name=CCY.AMOUNT, typeModifiers="1")
		MKT_CCY_AMT : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=38.0, core=true, t24Name=MKT.CCY.AMT, typeModifiers="1")
		/* Fields SCHEDULE.TYPE to VALUE.DATE form a related multi value set. Standard T24 rate field.
Validation Rules
A maximum of 11 characters may be entered. */
		RESET_RATE : T24BusinessTypes:R
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=39.0, core=true, t24Name=RESET.RATE)
		/* Fields SCHEDULE.TYPE to VALUE.DATE form a related multi value set.
Validation Rules
A maximum of 19 characters may be entered. */
		LCL_AMOUNT : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=40.0, core=true, t24Name=LCL.AMOUNT, typeModifiers="USD")
		/* Fields SCHEDULE.TYPE to VALUE.DATE form a related multi value set.
Validation Rules
A maximum of 19 characters may be entered. */
		AMOUNT_DIFF : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=41.0, core=true, t24Name=AMOUNT.DIFF, typeModifiers="1")
		/* Fields SCHEDULE.TYPE to VALUE.DATE form a related multi value set. Standard T24 date field.
Validation Rules
A maximum of 11 characters may be entered. */
		VALUE_DATE : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=42.0, core=true, t24Name=VALUE.DATE)
	}
	SWAP_BALANCES_HIST__POSITION_DATE
	{
		/* Fields POSITION.DATE to  form a related multi value set. Standard T24 date field.
Validation Rules
A maximum of 11 characters may be entered. */
		POSITION_DATE : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=52.0, core=true, t24Name=POSITION.DATE)
		/* Fields POSITION.DATE to  form a related multi value set.
Validation Rules
A maximum of 19 characters may be entered. */
		POSITION_FCY : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=53.0, core=true, t24Name=POSITION.FCY, typeModifiers="1")
		/* Fields POSITION.DATE to  form a related multi value set.
Validation Rules
A maximum of 19 characters may be entered. */
		POSITION_LCY : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=54.0, core=true, t24Name=POSITION.LCY, typeModifiers="USD")
	}
	/* The SWAP.BALANCES.SAVE file stores the last authorised versions of Swap balance records (SWAP.BALANCES).
For further information with regard to this file and its' associated fields, please see SWAP.BALANCES helptext. */
	SWAP_BALANCES_SAVE
		@t24:i (t24Name=SWAP.BALANCES.SAVE, level=F, type=L, allowedFunctions="L P S", localRefAllowed=false)
	{
		/* Standard T24 alphanumeric field.
Validation Rules
A maximum of 30 characters may be entered. */
		CONTRACT_NUMBER -> SW_Contract:SWAP_BALANCES_SAVE PK required
			@i18n:Label (en="Contract Number")
			@t24:i (align=L, inputBehaviour=I, maxLength=30, sysNumber=0.0, core=true, businessType=A, t24Name=CONTRACT.NUMBER)
		/* Standard T24 currency field.
Validation Rules
A maximum of 3 characters may be entered. */
		CURRENCY : T24BusinessTypes:CCY
			@i18n:Label (en=Ccy)
			@t24:i (align=L, inputBehaviour=I, maxLength=3, sysNumber=1.0, core=true, t24Name=CURRENCY)
		/* Validation Rules
A maximum of 2 characters may be entered. */
		NOTIONAL : T24BusinessTypes:A
			@t24:i (align=L, inputBehaviour=I, maxLength=2, sysNumber=2.0, core=true, t24Name=NOTIONAL)
		PRINCIPAL -> byValue SW_Contract:SWAP_BALANCES_SAVE__PRINCIPAL *
		EFFECTIVE_DATE -> byValue SW_Contract:SWAP_BALANCES_SAVE__EFFECTIVE_DATE *
		/* Validation Rules
A maximum of 19 characters may be entered. */
		INTEREST_AMOUNT : T24BusinessTypes:AMT
			@i18n:Label (en="Interest Amt")
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=9.0, core=true, t24Name=INTEREST.AMOUNT, typeModifiers="1")
		MKT_INT_AMOUNT : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=10.0, core=true, t24Name=MKT.INT.AMOUNT, typeModifiers="1")
		/* Standard T24 date field.
Validation Rules
A maximum of 11 characters may be entered. */
		START_INT_PERIOD : T24BusinessTypes:D
			@i18n:Label (en="Start Int Period")
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=11.0, core=true, t24Name=START.INT.PERIOD)
		/* Standard T24 date field.
Validation Rules
A maximum of 11 characters may be entered. */
		END_INT_PERIOD : T24BusinessTypes:D
			@i18n:Label (en="End Int Period")
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=12.0, core=true, t24Name=END.INT.PERIOD)
		TOT_INT_AMT : T24BusinessTypes:AMT
			@i18n:Label (en="Total Intt Amt")
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=13.0, core=true, t24Name=TOT.INT.AMT, typeModifiers="1")
		TOT_MKT_INT_AMT : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=14.0, core=true, t24Name=TOT.MKT.INT.AMT, typeModifiers="1")
		ACCR_FROM_DATE -> byValue SW_Contract:SWAP_BALANCES_SAVE__ACCR_FROM_DATE *
		AMORT_SCHEDULE -> byValue SW_Contract:SWAP_BALANCES_SAVE__AMORT_SCHEDULE *
		SCHEDULE_TYPE -> byValue SW_Contract:SWAP_BALANCES_SAVE__SCHEDULE_TYPE *
		/* Standard T24 date field.
Validation Rules
A maximum of 11 characters may be entered. */
		CRB_VALUE_DATE : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=43.0, core=true, t24Name=CRB.VALUE.DATE)
		/* Standard T24 date field.
Validation Rules
A maximum of 11 characters may be entered. */
		CRB_MATURITY_DATE : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=44.0, core=true, t24Name=CRB.MATURITY.DATE)
		/* Standard T24 date field.
Validation Rules
A maximum of 11 characters may be entered. */
		CRB_INTEREST_DATE : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=45.0, core=true, t24Name=CRB.INTEREST.DATE)
		/* Standard T24 alphanumeric field.
Validation Rules
A maximum of 16 characters may be entered. */
		CONSOL_KEY : T24BusinessTypes:A
			@i18n:Label (en="Consol Key")
			@t24:i (align=L, inputBehaviour=I, maxLength=16, sysNumber=46.0, core=true, t24Name=CONSOL.KEY)
		/* This field will only become populated when the leg of the swap contract has undergone the currency revaluation processing. The leg of the swap contract must meet all of the following criteria to undergo the currency revaluation processing;
1) The contract must be OFF balance sheet.   2) The leg must be non notional.   3) The currency of the leg must not be the local currency.
When a leg of a swap contract has undergone the currency revaluation processing, this field will hold the exchange rate which was applicable to the currency at the time the leg became non notional.
Validation Rules
Standard T24 rate field. */
		INITIAL_XRATE : T24BusinessTypes:R
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=47.0, core=true, t24Name=INITIAL.XRATE)
		/* It holds the principal outstanding in local currency.
All principal movements will update this field.
Validation Rules
1-19 type AMT (standard amount format) characters plus a decimal point. */
		OUTS_PRIN_LCY : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=48.0, core=true, t24Name=OUTS.PRIN.LCY, typeModifiers="USD")
		/* This field holds the net amount which has been posted as unrealised P&L in local currency.
The amount is derived by the formula:- Principal outstanding * (current.xrate - initial.xrate)
Validation Rules
1-19 type AMT (standard amount format) characters plus a decimal point. */
		CCY_REVAL_PL : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=49.0, core=true, t24Name=CCY.REVAL.PL, typeModifiers="USD")
		/* Validation Rules
A maximum of 19 characters may be entered. */
		NPV : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=50.0, core=true, t24Name=NPV, typeModifiers="1")
		/* Validation Rules
A maximum of 19 characters may be entered. */
		NPV_LCY : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=51.0, core=true, t24Name=NPV.LCY, typeModifiers="USD")
		POSITION_DATE -> byValue SW_Contract:SWAP_BALANCES_SAVE__POSITION_DATE *
		CONF_SENT : mml:Integer
			@t24:i (align=R, inputBehaviour=N, maxLength=55, sysNumber=55.0, core=true, t24Name=CONF.SENT)
		OLD_OUTS_PRIN_LCY : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=56.0, core=true, t24Name=OLD.OUTS.PRIN.LCY)
		NPV_BEF_ACC_ADJ : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=57.0, core=true, t24Name=NPV.BEF.ACC.ADJ)
		NPV_BEF_ADJ_LCY : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=58.0, core=true, t24Name=NPV.BEF.ADJ.LCY)
		NPV_INC_TDY_IP : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=59.0, core=true, t24Name=NPV.INC.TDY.IP)
		NPV_INC_TDY_IPLCY : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=60.0, core=true, t24Name=NPV.INC.TDY.IPLCY)
		CCY_REVAL_FCY : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=61.0, core=true, t24Name=CCY.REVAL.FCY)
	}
	SWAP_BALANCES_SAVE__PRINCIPAL
	{
		/* Fields PRINCIPAL to PRIN.DATE form a related multi value set.
Validation Rules
A maximum of 19 characters may be entered. */
		PRINCIPAL : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=3.0, core=true, t24Name=PRINCIPAL, typeModifiers="1")
		/* Fields PRINCIPAL to PRIN.DATE form a related multi value set. Standard T24 date field.
Validation Rules
A maximum of 11 characters may be entered. */
		PRIN_DATE : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=4.0, core=true, t24Name=PRIN.DATE)
	}
	SWAP_BALANCES_SAVE__EFFECTIVE_DATE
	{
		/* Fields EFFECTIVE.DATE to INTEREST.RATE form a related multi value set. Standard T24 date field.
Validation Rules
A maximum of 11 characters may be entered. */
		EFFECTIVE_DATE : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=5.0, core=true, t24Name=EFFECTIVE.DATE)
		/* Fields EFFECTIVE.DATE to INTEREST.RATE form a related multi value set. Standard T24 numeric field.
Validation Rules
A maximum of 4 characters may be entered. */
		INTEREST_KEY : mml:Integer
			@t24:i (align=R, inputBehaviour=I, maxLength=6, sysNumber=6.0, core=true, t24Name=INTEREST.KEY)
		/* Fields EFFECTIVE.DATE to INTEREST.RATE form a related multi value set. Standard T24 rate field.
Validation Rules
A maximum of 11 characters may be entered. */
		INTEREST_RATE : T24BusinessTypes:R
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=7.0, core=true, t24Name=INTEREST.RATE)
		MKT_INT_RATE : T24BusinessTypes:R
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=8.0, core=true, t24Name=MKT.INT.RATE)
	}
	SWAP_BALANCES_SAVE__ACCR_FROM_DATE
	{
		/* Fields ACCR.FROM.DATE to MKT.ACT.AMT form a related multi value set. Standard T24 date field.
Validation Rules
A maximum of 11 characters may be entered. */
		ACCR_FROM_DATE : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=15.0, core=true, t24Name=ACCR.FROM.DATE)
		/* Fields ACCR.FROM.DATE to MKT.ACT.AMT form a related multi value set. Standard T24 date field.
Validation Rules
A maximum of 11 characters may be entered. */
		ACCR_TO_DATE : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=16.0, core=true, t24Name=ACCR.TO.DATE)
		/* Fields ACCR.FROM.DATE to MKT.ACT.AMT form a related multi value set. Standard T24 numeric field.
Validation Rules
A maximum of 4 characters may be entered. */
		ACCR_DAYS : mml:Integer
			@t24:i (align=R, inputBehaviour=I, maxLength=4, sysNumber=17.0, core=true, t24Name=ACCR.DAYS)
		/* Fields ACCR.FROM.DATE to MKT.ACT.AMT form a related multi value set. Standard T24 amount field.
Validation Rules
A maximum of 19 characters may be entered. */
		ACCR_PRIN : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=18.0, core=true, t24Name=ACCR.PRIN)
		/* Fields ACCR.FROM.DATE to MKT.ACT.AMT form a related multi value set. Standard T24 rate field.
Validation Rules
A maximum of 11 characters may be entered. */
		ACCR_RATE : T24BusinessTypes:R
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=19.0, core=true, t24Name=ACCR.RATE)
		/* Fields ACCR.FROM.DATE to MKT.ACT.AMT form a related multi value set.
Validation Rules
A maximum of 19 characters may be entered. */
		ACCR_AMT : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=20.0, core=true, t24Name=ACCR.AMT, typeModifiers="1")
		/* Fields ACCR.FROM.DATE to MKT.ACT.AMT form a related multi value set. Standard T24 numeric field.
Validation Rules
A maximum of 19 characters may be entered. */
		ACCR_ACT_AMT : mml:Integer
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=21.0, core=true, t24Name=ACCR.ACT.AMT)
		MKT_ACCR_RATE : T24BusinessTypes:R
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=22.0, core=true, t24Name=MKT.ACCR.RATE)
		MKT_ACCR_AMT : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=23.0, core=true, t24Name=MKT.ACCR.AMT, typeModifiers="1")
		MKT_ACT_AMT : mml:Integer
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=24.0, core=true, t24Name=MKT.ACT.AMT)
	}
	SWAP_BALANCES_SAVE__AMORT_SCHEDULE
	{
		/* Fields AMORT.SCHEDULE to AMORT.TO.DATE form a related multi value set. Standard T24 alphanumeric field.
Validation Rules
A maximum of 12 characters may be entered. */
		AMORT_SCHEDULE : T24BusinessTypes:A
			@t24:i (align=L, inputBehaviour=I, maxLength=12, sysNumber=25.0, core=true, t24Name=AMORT.SCHEDULE)
		/* Fields AMORT.SCHEDULE to AMORT.TO.DATE form a related multi value set.
Validation Rules
A maximum of 19 characters may be entered. */
		AMORT_AMOUNT : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=26.0, core=true, t24Name=AMORT.AMOUNT, typeModifiers="1")
		/* Fields AMORT.SCHEDULE to AMORT.TO.DATE form a related multi value set. Standard T24 date field.
Validation Rules
A maximum of 11 characters may be entered. */
		AMORT_START : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=27.0, core=true, t24Name=AMORT.START)
		/* Fields AMORT.SCHEDULE to AMORT.TO.DATE form a related multi value set. Standard T24 date field.
Validation Rules
A maximum of 11 characters may be entered. */
		AMORT_END : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=28.0, core=true, t24Name=AMORT.END)
		/* Fields AMORT.SCHEDULE to AMORT.TO.DATE form a related multi value set. Standard T24 amount field.
Validation Rules
A maximum of 19 characters may be entered. */
		AMORT_TO_DATE : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=29.0, core=true, t24Name=AMORT.TO.DATE)
	}
	SWAP_BALANCES_SAVE__SCHEDULE_TYPE
	{
		/* Fields SCHEDULE.TYPE to VALUE.DATE form a related multi value set. Standard T24 alphanumeric field.
Validation Rules
A maximum of 12 characters may be entered.
Must be the key to a valid entry on the SWAP.SCHEDULE.TYPE file. */
		SCHEDULE_TYPE -> SW_Schedules:SWAP_SCHEDULE_TYPE
			@t24:i (align=L, inputBehaviour=I, maxLength=12, sysNumber=30.0, core=true, businessType=A, t24Name=SCHEDULE.TYPE)
		/* Fields SCHEDULE.TYPE to VALUE.DATE form a related multi value set.
Validation Rules
A maximum of 12 characters may be entered. */
		CHARGE_CODE : T24BusinessTypes:CHG
			@t24:i (align=R, inputBehaviour=I, maxLength=12, sysNumber=31.0, core=true, t24Name=CHARGE.CODE, typeModifiers="CHGCOMDTAX")
		/* Fields SCHEDULE.TYPE to VALUE.DATE form a related multi value set. Standard T24 date field.
Validation Rules
A maximum of 11 characters may be entered. */
		SCHEDULE_DATE : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=32.0, core=true, t24Name=SCHEDULE.DATE)
		/* Fields SCHEDULE.TYPE to VALUE.DATE form a related multi value set. Standard T24 date field.
Validation Rules
A maximum of 11 characters may be entered. */
		PROCESS_DATE : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=33.0, core=true, t24Name=PROCESS.DATE)
		/* Fields SCHEDULE.TYPE to VALUE.DATE form a related multi value set. Standard T24 date field.
Validation Rules
A maximum of 11 characters may be entered. */
		PERIOD_START : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=34.0, core=true, t24Name=PERIOD.START)
		/* Fields SCHEDULE.TYPE to VALUE.DATE form a related multi value set. Standard T24 date field.
Validation Rules
A maximum of 11 characters may be entered. */
		PERIOD_END : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=35.0, core=true, t24Name=PERIOD.END)
		/* Fields SCHEDULE.TYPE to VALUE.DATE form a related multi value set. Standard T24 date field.
Validation Rules
A maximum of 11 characters may be entered. */
		SCHED_EFF_DATE : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=36.0, core=true, t24Name=SCHED.EFF.DATE)
		/* Fields SCHEDULE.TYPE to VALUE.DATE form a related multi value set.
Validation Rules
A maximum of 19 characters may be entered. */
		CCY_AMOUNT : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=37.0, core=true, t24Name=CCY.AMOUNT, typeModifiers="1")
		MKT_CCY_AMT : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=38.0, core=true, t24Name=MKT.CCY.AMT, typeModifiers="1")
		/* Fields SCHEDULE.TYPE to VALUE.DATE form a related multi value set. Standard T24 rate field.
Validation Rules
A maximum of 11 characters may be entered. */
		RESET_RATE : T24BusinessTypes:R
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=39.0, core=true, t24Name=RESET.RATE)
		/* Fields SCHEDULE.TYPE to VALUE.DATE form a related multi value set.
Validation Rules
A maximum of 19 characters may be entered. */
		LCL_AMOUNT : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=40.0, core=true, t24Name=LCL.AMOUNT, typeModifiers="USD")
		/* Fields SCHEDULE.TYPE to VALUE.DATE form a related multi value set.
Validation Rules
A maximum of 19 characters may be entered. */
		AMOUNT_DIFF : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=41.0, core=true, t24Name=AMOUNT.DIFF, typeModifiers="1")
		/* Fields SCHEDULE.TYPE to VALUE.DATE form a related multi value set. Standard T24 date field.
Validation Rules
A maximum of 11 characters may be entered. */
		VALUE_DATE : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=42.0, core=true, t24Name=VALUE.DATE)
	}
	SWAP_BALANCES_SAVE__POSITION_DATE
	{
		/* Fields POSITION.DATE to  form a related multi value set. Standard T24 date field.
Validation Rules
A maximum of 11 characters may be entered. */
		POSITION_DATE : T24BusinessTypes:D
			@t24:i (align=R, inputBehaviour=I, maxLength=11, sysNumber=52.0, core=true, t24Name=POSITION.DATE)
		/* Fields POSITION.DATE to  form a related multi value set.
Validation Rules
A maximum of 19 characters may be entered. */
		POSITION_FCY : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=53.0, core=true, t24Name=POSITION.FCY, typeModifiers="1")
		/* Fields POSITION.DATE to  form a related multi value set.
Validation Rules
A maximum of 19 characters may be entered. */
		POSITION_LCY : T24BusinessTypes:AMT
			@t24:i (align=R, inputBehaviour=I, maxLength=19, sysNumber=54.0, core=true, t24Name=POSITION.LCY, typeModifiers="USD")
	}
	SWAP_CUSTOMER_CONCAT
		@t24:i (t24Name=SWAP.CUSTOMER.CONCAT, level=F, type=T, allowedFunctions="L P S", localRefAllowed=false)
	{
		/* Standard T24 customer field.
Validation Rules
A maximum of 10 characters may be entered.
Must be the key to a valid entry on the CUSTOMER file. */
		CUSTOMER -> SW_Contract:SWAP_CUSTOMER_CONCAT PK required
			@i18n:Label (en=Customer)
			@i18n:Tooltip (en="Customer Liability")
			@t24:i (align=R, inputBehaviour=I, maxLength=10, sysNumber=0.0, core=true, businessType=CUS, t24Name=CUSTOMER)
		/* Standard T24 alphanumeric field.
Validation Rules
A maximum of 12 characters may be entered.
Must be the key to a valid entry on the SWAP file. */
		CONTRACT_NUMBER -> SW_Contract:SWAP
			@i18n:Label (en="Contract Number")
			@t24:i (align=L, inputBehaviour=I, maxLength=12, sysNumber=1.0, core=true, businessType=A, t24Name=CONTRACT.NUMBER)
	}
}
Enumerations {
	SWAP__REVALUATION_TYPE : mml:string
		ZERO_CPN_RATE=ZERO.CPN.RATE
		WITH_RX=WITH.RX
	SWAP__TRADE_TYPE : mml:string
		TRADE=TRADE
		HEDGE=HEDGE
	SWAP__NET_PAYMENTS : mml:string
		Y=Y
		NO=NO
	SWAP__TREASURY_CUSTOMER : mml:string
		NO=NO
	SWAP__AS_FIXED_INTEREST : mml:string
		Y=Y
		NO=NO
	SWAP__AS_DAY_CONVENTION : mml:string
		FOLLOWING=FOLLOWING
		MODIFIED=MODIFIED
		PRECEDING=PRECEDING
	SWAP__AS_DATE_ADJUSTMENT : mml:string
		VALUE=VALUE
		PERIOD=PERIOD
	SWAP__AS_TYPE__AS_ADVICE_SENT : mml:string
		Y=Y
	SWAP__LB_FIXED_INTEREST : mml:string
		Y=Y
		NO=NO
	SWAP__LB_DAY_CONVENTION : mml:string
		FOLLOWING=FOLLOWING
		MODIFIED=MODIFIED
		PRECEDING=PRECEDING
	SWAP__LB_DATE_ADJUSTMENT : mml:string
		VALUE=VALUE
		PERIOD=PERIOD
	SWAP__LB_TYPE__LB_ADVICE_SENT : mml:string
		Y=Y
	SWAP__SET_CURRENCY__SET_PAY_RECEIPT : mml:string
		PAYMENT=PAYMENT
		RECEIPT=RECEIPT
	SWAP__SET_CURRENCY__SET_TYPE : mml:string
		PRINCIPAL=PRINCIPAL
		INTEREST=INTEREST
	SWAP__BROKERAGE_BASE : mml:string
		ASSET=ASSET
		LIABILITY=LIABILITY
	SWAP__SEND_PAYMENT : mml:string
		Y=Y
		NO=NO
	SWAP__SEND_CONFIRMATION : mml:string
		Y=Y
		NO=NO
	SWAP__CONFIRM_BY_CUST : mml:string
		ONESIDED=ONESIDED
	SWAP__CONFIRM_BY_BROKER : mml:string
		ONESIDED=ONESIDED
	SWAP__CONTRACT_STATUS : mml:string
		CUR=CUR
		MAT=MAT
	SWAP__ASSET_STATUS : mml:string
		CUR=CUR
		MAT=MAT
	SWAP__LIABILITY_STATUS : mml:string
		CUR=CUR
		MAT=MAT
	SWAP__ACTIVITY__ACT_LEG_TYPE : mml:string
		A=A
		L=L
	SWAP__SEND_ADVICE : mml:string
		Y=Y
		NO=NO
	SWAP__LIQUIDATION_MODE : mml:string
		AUTOMATIC=AUTOMATIC
		MANUAL=MANUAL
		SEMI__AUTOMATIC=SEMI-AUTOMATIC
	SWAP__SOD_MAT : mml:string
		YES=YES
		NO=NO
	SWAP__FLEX_PRIN_PAYMENT : mml:string
		YES=YES
		NO=NO
	SWAP__NEGATIVE_RATES : mml:string
		YES=YES
		NO=NO
	SWAP__MAT_PAYMENT_DATE : mml:string
		YES=YES
		NO=NO
	SWAP_BALANCES__NOTIONAL : mml:string
		Y=Y
		NO=NO
	SWAP_BALANCES_HIST__NOTIONAL : mml:string
		Y=Y
		NO=NO
	SWAP_BALANCES_SAVE__NOTIONAL : mml:string
		Y=Y
		NO=NO
}